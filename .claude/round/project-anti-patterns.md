# Project-Specific Anti-Patterns — SMLMClustering

Project-local additions to the canonical anti-pattern list. Read in addition to `.claude/round/anti-patterns.md` — both files are consulted during Phase 3.

Add project-specific rules here as they emerge from evidence in your rounds. Keep the format consistent with the canonical file: a numbered rule, then one paragraph explaining when it applies and why.

---

<!-- Examples of what goes here (not universal, project-dependent):

## 1. Do not add files to references/

That directory is read-only canonical data. New reference material belongs in docs/ or project-anti-patterns.md itself.

## 2. Do not commit unstripped binaries

Our CI size budget is small. Stripped binaries only (see scripts/strip-release.sh).

## 3. Do not commit model weights

Weights are tracked in the artifacts bucket, not in git.

-->

(add project-specific anti-patterns here)
