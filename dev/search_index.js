var documenterSearchIndex = {"docs":
[{"category":"section","location":"methods/voronoi_density/#Voronoi-density","page":"Voronoi density","text":"A cluster_statistics backend (selected by VoronoiDensityConfig) that reports each emitter's Voronoi cell area A_i and its local density rho_i = 1A_i, intended as a feature for downstream thresholding (Otsu / GMM on logrho, a fixed cutoff, etc.). This is the read-only sibling of the Voronoi (SR-Tesseler) labeling backend: VoronoiConfig consumes the same per-emitter areas to assign clusters, whereas VoronoiDensityConfig only computes the density feature and assigns no cluster labels.\n\n(Image: Per-emitter Voronoi density)\n\nLeft: the per-emitter Voronoi density feature this backend computes (bright = dense). Right: the same cell areas drive the SR-Tesseler labeling backend.","title":"Voronoi density"},{"category":"section","location":"methods/voronoi_density/#Concept","page":"Voronoi density","text":"The Voronoi tessellation of a point set partitions the plane into one convex cell per generator — the region closer to that generator than to any other. The area of an emitter's cell is small where neighbors crowd in and large where they are sparse, so the cell area is a parameter-free inverse local-density estimate: rho_i = 1A_i. Unlike a fixed-radius or k-NN density, the cell adapts its own scale to the local point spacing, which is why Voronoi density is a robust density feature for the highly heterogeneous point clouds typical of SMLM. The usual use is cell-structure masking: separating dense membrane / structure regions from sparse background by thresholding logrho (e.g. Otsu or a two-component GMM), as in SR-Tesseler.","title":"Concept"},{"category":"section","location":"methods/voronoi_density/#How-it-works","page":"Voronoi density","text":"For each group of emitters (one dataset when per_dataset = true, all emitters pooled otherwise), the backend builds the Voronoi tessellation of the emitter (x y) coordinates (in µm) using DelaunayTriangulation.jl. The tessellation is clipped to the convex hull of the group, so every generator receives a finite cell area (unbounded hull cells would otherwise have infinite area). For each emitter i the cell area and density are\n\nA_i = operatornameareabigl(textcell_ibigr)  mu m^2\nqquad\nrho_i = frac1A_i  mu m^-2\n\nEach group is tessellated independently; the per-group area and density vectors are then stitched back into the original (flat) emitter order, so the i-th entry always corresponds to smld.emitters[i] regardless of how the emitters were grouped. The summary scalar is the median density over all emitters that received a valid cell.","title":"How it works"},{"category":"section","location":"methods/voronoi_density/#Configuration","page":"Voronoi density","text":"VoronoiDensityConfig <: AbstractStatisticsConfig. Both fields are keyword arguments with defaults:\n\nfield default unit meaning\nuse_3d false — must be false; Voronoi tessellation is 2D only — true raises ArgumentError\nper_dataset true — when true, tessellate each dataset independently; when false, pool all emitters into one tessellation. Per-emitter outputs stay flat in original emitter order in either case\n\nusing SMLMClustering\n\ncfg = VoronoiDensityConfig(\n    use_3d      = false,    # 2D only — same constraint as VoronoiConfig\n    per_dataset = true,     # tessellate each dataset independently\n)\n(_, info) = cluster_statistics(smld, cfg)\n\ninfo.statistic        # median density across valid cells (Float64, µm⁻²)\ninfo.statistic_name   # :median_density\ninfo.algorithm        # :voronoi_density\n\nρ = info.extras[:density_per_emitter]   # Vector{Float64}, length == n_locs_in, µm⁻²\nA = info.extras[:area_per_emitter]      # Vector{Float64}, length == n_locs_in, µm²\n\nprintln(\"median density = \", round(info.statistic, digits = 2), \" µm⁻²\")","title":"Configuration"},{"category":"section","location":"methods/voronoi_density/#Output-and-interpretation","page":"Voronoi density","text":"cluster_statistics returns (smld, info) where info is a ClusterStatisticsInfo. The SMLD is the same, unmodified reference as the input — this backend writes nothing onto it.\n\ninfo.statistic — the median of all non-NaN per-emitter densities (statistic_name = :median_density), a single-number summary of overall density in µm⁻². It is NaN if no group produced any valid density.\ninfo.extras[:density_per_emitter] — Vector{Float64} of length n_locs_in, in µm⁻².\ninfo.extras[:area_per_emitter] — Vector{Float64} of length n_locs_in, in µm².\n\nBoth extras vectors are flat in original emitter order, not grouped by dataset: ρ[i] and A[i] correspond to smld.emitters[i]. Feed density_per_emitter (or log of it) into your own thresholding step — Otsu, a GMM split, or a fixed cutoff — to mask dense structure.","title":"Output & interpretation"},{"category":"section","location":"methods/voronoi_density/#Notes-and-caveats","page":"Voronoi density","text":"2D only. use_3d = true raises ArgumentError; DelaunayTriangulation.jl does not provide a 3D Voronoi tessellation.\nSmall groups. Any group with fewer than 3 points cannot be tessellated; those emitters receive NaN density and NaN area while other groups proceed normally. An empty SMLD yields empty extras vectors and statistic = NaN.\nDuplicate coordinates. A group containing exact-duplicate (x y) coordinates raises ArgumentError before triangulation (duplicate generators are treated as a boundary-input issue, not a data-shape edge case — mirroring VoronoiConfig's guard). Deduplicate localizations beforehand.\nHull clipping. Because cells are clipped to the convex hull, emitters on or near the hull boundary have areas smaller than their infinite-plane Voronoi cell would be; their density is correspondingly higher than the interior trend.","title":"Notes & caveats"},{"category":"section","location":"methods/voronoi_density/#References","page":"Voronoi density","text":"Levet, F. et al. SR-Tesseler: a method to segment and quantify localization-based super-resolution microscopy data. Nature Methods 12, 1065–1071 (2015). doi:10.1038/nmeth.3579 — Voronoi-based density estimation and segmentation for SMLM.\nDelaunayTriangulation.jl — the pure-Julia Delaunay/Voronoi engine used to build the tessellation.","title":"References"},{"category":"section","location":"methods/hdbscan/#HDBSCAN","page":"HDBSCAN","text":"Hierarchical density-based clustering (Campello, Moulavi & Sander 2013) exposed as a cluster labeling backend through HDBSCANConfig. Unlike DBSCAN it needs no single global radius ε: it builds a hierarchy over mutual-reachability distance, condenses it by minimum cluster size, and extracts the most stable flat clusters — so clusters of differing local density can be recovered in one pass. The implementation is pure Julia (no external HDBSCAN library) built on NearestNeighbors.KDTree.\n\n(Image: HDBSCAN vs single-eps DBSCAN on mixed density)\n\nTwo clusters of very different density. A single DBSCAN eps tuned for the dense cluster misses the diffuse one entirely (left); HDBSCAN's adaptive density recovers both (right).","title":"HDBSCAN"},{"category":"section","location":"methods/hdbscan/#Concept","page":"HDBSCAN","text":"DBSCAN draws clusters at one density level set by eps_nm: a clump that is dense in one region and loose in another forces a single compromise radius. HDBSCAN replaces that single cut with a hierarchy of cuts:\n\nCore distance d_textcore(p) turns each point's local neighbor spacing into a density estimate — the distance to its k-th nearest neighbor (here k = min_points). Sparse points have large core distances; dense points have small ones.\nMutual reachability distance d_textmreach(ab) inflates the raw Euclidean distance between two points by their core distances, so the graph that links points is \"smoothed\" against low-density bridges and single-link chaining.\nA minimum spanning tree (MST) over d_textmreach gives a single-linkage hierarchy: a dendrogram parameterised by lambda = 1d_textmreach, where high lambda means high density.\nThe dendrogram is condensed using min_cluster_size: only splits where both branches are large enough survive as real sub-clusters; smaller branches \"fall out\" as noise of the parent. The result is the condensed cluster tree.\nEach candidate cluster gets a stability (persistence) score, and a flat partition is extracted by selecting the set of tree nodes that maximises total stability (excess of mass), rather than cutting the dendrogram at one height.\n\nPrefer HDBSCAN over DBSCAN when clusters have varying density, when a good global eps_nm does not exist, or when you want the algorithm to decide how many clusters there are. The cost is more parameters with less direct physical meaning (counts, not nanometres) and a heavier compute path than DBSCAN.","title":"Concept"},{"category":"section","location":"methods/hdbscan/#How-it-works","page":"HDBSCAN","text":"The backend reproduces the Campello/Moulavi/Sander 2013 algorithm directly on the d times n coordinate matrix (microns; d = 2, or d = 3 when use_3d=true).\n\n1. Core distance. Build a Euclidean KDTree and, for each point p, query its nearest neighbors. With m = min_points (clamped to n-1), the core distance is the distance to the m-th neighbor, excluding p itself:\n\nd_textcore(p) = bigllVert p - p_(m) bigrrVert_2\nqquad m = textttmin_points\n\n2. Mutual reachability. For an edge (ab),\n\nd_textmreach(ab) = maxbigl(d_textcore(a) d_textcore(b)\nlVert a - b rVert_2bigr)\n\n3. Minimum spanning tree. A full d_textmreach graph is O(n^2), so the code builds a sparse k-NN graph with k = knn_graph_k (clamped to n-1), weights each edge by d_textmreach, and runs Kruskal's algorithm with union-find to get the MST. Because a k-NN graph can be disconnected when clusters sit far apart, a bridge-repair step then queries the KD-tree with a progressively doubling k (up to n-1) to find the cheapest mutual-reachability edge joining two components, adds those bridges, and repeats until the tree spans all n points. Only a genuinely unbridgeable graph raises an error.\n\n4. Single-linkage hierarchy. MST edges are merged in ascending weight; each merge is a dendrogram node at density level\n\nlambda = frac1d_textmreach\n\n(coincident points, d_textmreach=0, give lambda = infty).\n\n5. Condense by min_cluster_size. Walking the dendrogram from the root, at each split with child sizes n_L n_R:\n\nif n_L ge min_cluster_size and n_R ge min_cluster_size, the parent dies and two child clusters are born at this lambda;\nif only one side is large enough, the small side falls out (its points become noise of the parent at this lambda) and the large side continues as the same cluster;\nif neither side is large enough, the cluster dies and both branches' points are its stable members up to this lambda.\n\n6. Stability (persistence). Each cluster C accumulates, over every fall-out event e, the density \"depth\" times the number of points n_e that fell:\n\nS(C) = sum_e in textfall events of  C\nbigl(lambda_e - lambda_textbirth(C)bigr) n_e \n\n7. Flat extraction. Two selection rules are available:\n\n:eom (excess of mass, the default and canonical HDBSCAN rule): walk the condensed tree bottom-up; keep C if S(C) ge sum_textchildren S(textbest), otherwise propagate the children's selection upward. The root (the whole connected mass) is a candidate only when allow_single_cluster=true.\n:leaf: select every leaf of the condensed tree (finer clusters, akin to DBSCAN at a locally varying ε).\n\nEach point is finally assigned to its deepest selected ancestor cluster; points with no selected ancestor are labelled noise (id = 0).","title":"How it works"},{"category":"section","location":"methods/hdbscan/#Configuration","page":"HDBSCAN","text":"HDBSCANConfig <: AbstractClusterConfig. All knobs are integer counts or symbols — note there are no length parameters in nm or µm (the geometry is set entirely by min_points / min_cluster_size, in contrast to DBSCAN's eps_nm).\n\nfield default unit meaning\nmin_points 5 neighbors (count) k for the core-distance: distance to the min_points-th nearest neighbor. Larger ⇒ more conservative (smoother density, fewer clusters).\nmin_cluster_size nothing → min_points emitters (count) minimum size of a cluster in the condensed tree. When nothing, falls back to min_points. Effective value must be ≥ 2.\nknn_graph_k 30 neighbors (count) width k of the sparse k-NN graph used as the MST scaffold. If a k leaves the graph disconnected the backend auto-bridges components (expanding k up to n-1); larger k avoids that repair work.\ncluster_selection_method :eom — :eom (excess of mass; canonical) or :leaf (all condensed-tree leaves).\nallow_single_cluster false — when true, the root (whole connected mass) is an EOM candidate, so single-blob data returns one cluster instead of zero. Matches the Python hdbscan default.\nuse_3d false — cluster in (xyz); requires 3D emitters (e.g. Emitter3DFit).\nper_dataset true — cluster within each dataset index independently; (dataset, id) then identifies a cluster across a multi-dataset SMLD.\nremove_unclustered false — drop noise emitters (id == 0) from the returned SMLD.\n\nValidation at dispatch entry: min_points ≥ 1, knn_graph_k ≥ 1, cluster_selection_method ∈ (:eom, :leaf), and the effective min_cluster_size ≥ 2 (otherwise an ArgumentError).\n\nusing SMLMClustering\n\n# Default-ish run: density set by min_points, EOM extraction.\ncfg = HDBSCANConfig(min_points = 10, min_cluster_size = 20, knn_graph_k = 50)\nsmld_out, info = cluster(smld, cfg)\n\nprintln(info)                      # ClusterInfo(.../... clustered, K clusters, ...)\nlabels      = [e.id for e in smld_out.emitters]            # 0 = noise, 1..K\npersistence = smld_out.metadata[\"hdbscan_cluster_persistence\"]   # one per cluster\nbirth_λ     = smld_out.metadata[\"hdbscan_cluster_lambda_birth\"]  # one per cluster\n\n# Finer leaf clusters, allow a single whole-data cluster, 3D:\ncfg3 = HDBSCANConfig(min_points = 15, cluster_selection_method = :leaf,\n                     allow_single_cluster = true, use_3d = true)\nsmld3, info3 = cluster(smld, cfg3)","title":"Configuration"},{"category":"section","location":"methods/hdbscan/#Output-and-interpretation","page":"HDBSCAN","text":"cluster(smld, ::HDBSCANConfig) follows the shared interface and returns (smld_out, info::ClusterInfo):\n\nLabels. Cluster ids are written to emitter.id on the deep-copied output SMLD: 0 = noise, 1..K = clusters. With per_dataset=true ids are local to each dataset, so (dataset, id) is the unique key. The input SMLD is never modified.\nClusterInfo carries n_locs_in, n_clustered, n_noise, n_clusters, cluster_sizes (length n_clusters; under per_dataset the per-dataset sizes are concatenated in dataset order — see below), algorithm = :hdbscan, and elapsed_s.\nHDBSCAN-specific metadata on smld_out.metadata:\n\"hdbscan_cluster_persistence\"::Vector{Float64} — per-cluster stability S(C);\n\"hdbscan_cluster_lambda_birth\"::Vector{Float64} — per-cluster birth lambda (units µm⁻¹, the density level at which the cluster appeared).\nBoth are flat vectors of length n_clusters in cluster-id order, concatenated across datasets in per_dataset order — i.e. the first n_clusters of dataset 1, then dataset 2, and so on, matching cluster_sizes. Higher persistence ⇒ a cluster that survives over a wider density range and is the more trustworthy structure.","title":"Output & interpretation"},{"category":"section","location":"methods/hdbscan/#Notes-and-caveats","page":"HDBSCAN","text":"2D and 3D. Both are supported via use_3d; 3D requires emitters with a :z property, otherwise coordinate extraction errors.\nCounts, not lengths. All geometric behaviour comes from min_points and min_cluster_size — there is no eps_nm. The natural scale parameter is the density (core distance), set implicitly by min_points.\nmin_points vs min_cluster_size. min_points controls the density estimate (the k-th-NN core distance); min_cluster_size controls how many points make a cluster in the condensed tree. They default to the same value but tune different things — raise min_points to smooth density, raise min_cluster_size to merge away small sub-clusters.\nknn_graph_k and connectivity. Too small a k triggers the bridge-repair path (correct but slower); the doubling search runs up to k = n-1, and only a truly unbridgeable graph raises \"cannot bridge components\". Raise knn_graph_k for well-separated multi-cluster data to skip repairs.\nEmpty / tiny groups. A group with n = 0 yields no clusters; a group with n  effective min_cluster_size is returned entirely as noise (no error).\nHalo trimming (halo_trim_frac). Raw HDBSCAN* labels every point under a selected cluster's branch, which lets a diffuse cluster sweep in density-connected background far beyond its real extent. By default this backend trims that halo: a point that fell out of its cluster near the cluster's birth (weakly attached) is returned as noise, so a cluster's members track its physical extent — the radius where its density crosses the background level. Set halo_trim_frac = 0 for the raw, un-trimmed HDBSCAN* labels; raise it to trim more aggressively.\nCoincident points. Duplicate coordinates give d_textmreach = 0 and lambda = infty; these are handled (the merge level is treated as infinite density) rather than erroring, unlike the Voronoi backend's duplicate guard.\nEOM returns nothing on a single blob. With the default :eom and allow_single_cluster=false, data that is one tight mass with no real internal split yields zero clusters; set allow_single_cluster=true to recover the whole mass as one cluster.\nCompute. Per group the cost is dominated by KD-tree neighbor queries plus the MST build (Kruskal over the k-NN edge set); it scales moderately — heavier than DBSCAN, lighter than the O(n^2) Hierarchical backend.","title":"Notes & caveats"},{"category":"section","location":"methods/hdbscan/#References","page":"HDBSCAN","text":"R. J. G. B. Campello, D. Moulavi, J. Sander, \"Density-Based Clustering Based on Hierarchical Density Estimates\", PAKDD 2013, LNCS 7819, pp. 160–172.\nR. J. G. B. Campello, D. Moulavi, A. Zimek, J. Sander, \"Hierarchical Density Estimates for Data Clustering, Visualization, and Outlier Detection\", ACM Transactions on Knowledge Discovery from Data 10(1):5, 2015 — the journal extension formalising cluster stability / excess of mass.\nL. McInnes, J. Healy, S. Astels, \"hdbscan: Hierarchical density based clustering\", Journal of Open Source Software 2(11):205, 2017 — the reference HDBSCAN* / :eom vs :leaf selection and allow_single_cluster semantics this backend mirrors.\nNearest-neighbor queries use NearestNeighbors.jl (KDTree, Euclidean metric).","title":"References"},{"category":"section","location":"methods/local_contrast/#Local-contrast","page":"Local contrast","text":"A cluster_statistics feature, configured by LocalContrastFeature, that computes a per-emitter local-density contrast: each point's fine-scale kNN log-density minus the median fine-scale log-density over a larger surrounding neighborhood. Positive values mark points that are denser than their local background; the result is a per-emitter feature vector the caller thresholds directly.\n\n(Image: Local contrast cancels a density gradient)\n\nA baseline density gradient (sparse left → dense right) hides three clusters in the raw kNN density (left). Subtracting a local baseline cancels the gradient, so the clusters stand out (right).","title":"Local contrast"},{"category":"section","location":"methods/local_contrast/#Concept","page":"Local contrast","text":"A raw density estimate cannot tell a point in a compact structure apart from a point in a uniformly dense region — both report a high absolute density. Local contrast captures the elevation of a point's density above the density of its own surroundings: the same number of neighbors packed into a tight core gives a large contrast, while neighbors spread evenly over the baseline give a contrast near zero.\n\nThis matters because the absolute-density classifier (Otsu / GMM on log density) calls a point \"structure\" whenever its density exceeds a single global cutoff. When the baseline density varies across the field of view — cell-edge thinning, illumination falloff, a biological gradient — that one cutoff is wrong on at least one side of the cell. Subtracting a coarse local baseline cancels the gradient, so the feature fires only where a point is denser than its immediate neighborhood rather than denser than the cell-wide average. The per-emitter vector is meant to be thresholded into a foreground mask, e.g. as the seed/support gates of a point-hysteresis seed-and-grow on clouds with non-stationary baseline density.","title":"Concept"},{"category":"section","location":"methods/local_contrast/#How-it-works","page":"Local contrast","text":"Coordinates are taken in microns (µm) from the emitters (2D (x, y), or (x, y, z) when use_3d=true). A KDTree is built over the coordinate set (per dataset or pooled, see Configuration). Write k_d for density_k and k_b for background_k.\n\n1. Fine kNN log-density. For each emitter i, let r_k be the Euclidean distance to its k_d-th nearest neighbor (excluding the point itself). The kNN density estimate normalizes the neighbor count by the disk area pi r_k^2, and the feature stores its natural log:\n\nf_i = logleft(frack_dpi r_k^2right)\n\nThe log keeps the downstream median and threshold comparisons on a comparable scale across a cell. If r_k = 0 (coincident coordinates), f_i is set to NaN.\n\n2. Local baseline. For each emitter i, take the median of f_j over its k_b nearest neighbors (excluding self, skipping any non-finite f_j). The median (rather than the mean) tolerates a small number of locally elevated neighbors without dragging the baseline up.\n\n3. Contrast. The per-emitter feature is the difference\n\nc_i = f_i - operatornamemedian_j in k_btext-NN(i) f_j\n\nBecause it is a difference of logs, c_i is the log-ratio of the point's fine density to its local baseline density: c_i  0 means point i is denser than its surroundings, c_i approx 0 means it matches the baseline, and c_i  0 means it is sparser. The value is reported in nats (natural-log scale).","title":"How it works"},{"category":"section","location":"methods/local_contrast/#Configuration","page":"Local contrast","text":"LocalContrastFeature is a Base.@kwdef struct <: AbstractStatisticsConfig:\n\nfield default unit meaning\ndensity_k 200 count fine-scale neighbor count k_d for the per-point log-density; sets the spatial scale of the signal\nbackground_k 2000 count coarse-scale neighbor count k_b for the local baseline; sets the spatial scale of the baseline. Must be > density_k\nuse_3d false — if true, build the KDTree over (x, y, z) instead of (x, y)\nper_dataset false — if true, compute each dataset independently (its own KDTree); per-emitter outputs are stitched back into original emitter order either way\n\nValidation happens at the cluster_statistics call: background_k > density_k and density_k ≥ 1 are required, otherwise an ArgumentError is raised (the baseline must be coarser than the signal).\n\nusing SMLMClustering\nusing Statistics   # for `quantile` in the thresholding example below\n\n# smld :: SMLMData.BasicSMLD\n(_, info) = cluster_statistics(smld, LocalContrastFeature(density_k=200, background_k=2000))\n\ninfo.statistic        # median of the finite per-emitter contrasts (scalar summary)\ninfo.statistic_name   # :median_local_contrast\ninfo.algorithm        # :local_contrast\n\ncontrast = info.extras[:contrast_per_emitter]      # Vector{Float64}, per emitter\nfine     = info.extras[:log_density_per_emitter]   # Vector{Float64}, the fine f_i\n\n# Threshold the per-emitter feature directly, e.g. seed/support gates for\n# hysteresis seed-and-grow on a non-stationary baseline:\nfine_floor = quantile(filter(isfinite, fine), 0.35)\nseed    = isfinite.(contrast) .& (contrast .> 0.25)  .& (fine .> fine_floor)\nsupport = isfinite.(contrast) .& (contrast .> -0.05) .& (fine .> fine_floor)\n(smld_out, _) = cluster(smld, PointHysteresisConfig(graph_k=12, min_points=150);\n                        seed=seed, support=support)","title":"Configuration"},{"category":"section","location":"methods/local_contrast/#Output-and-interpretation","page":"Local contrast","text":"cluster_statistics returns (smld, info) where smld is the unmodified input (the statistics interface writes nothing onto the SMLD) and info is a ClusterStatisticsInfo:\n\ninfo.statistic — the median of the finite contrast values pooled across all groups; a single-scalar summary of the run. It is NaN when no group has enough points to compute the feature.\ninfo.statistic_name — :median_local_contrast.\ninfo.algorithm — :local_contrast.\ninfo.extras[:contrast_per_emitter] — Vector{Float64} of length n_locs_in, the per-emitter contrast c_i, in original emitter order. This is the primary output the caller thresholds.\ninfo.extras[:log_density_per_emitter] — Vector{Float64} of length n_locs_in, the fine kNN log-density f_i, also in original emitter order. Useful as a complementary absolute-density gate (e.g. require f_i above a quantile floor in addition to a positive contrast).\n\nInterpretation: a high positive contrast is a point sitting in a local density peak (a candidate structure point); a contrast near zero is a point whose density matches its surroundings (uniform background, regardless of how dense that background is); a negative contrast is a point sparser than its neighborhood. Because the baseline is local, the same contrast threshold applies on both the dense and the thin side of a density gradient.","title":"Output & interpretation"},{"category":"section","location":"methods/local_contrast/#Notes-and-caveats","page":"Local contrast","text":"2D / 3D. Both are supported via use_3d. The KDTree and neighbor distances are taken in the chosen dimensionality, but the density normalization is always the 2D disk area pi r_k^2 in f_i — even when use_3d=true. The contrast difference still behaves sensibly, but the absolute log_density_per_emitter scale is a 2D-area form, not a true 3D (volume) density.\nToo-small groups. A group with n le k_d emitters cannot be evaluated; those emitters receive NaN for both contrast and log-density (avoids a degenerate kNN query against a too-small set).\nbackground_k ≥ n in a group. k_b is clamped to n-1 for that group, so the feature degrades gracefully to \"log-density minus the group median,\" which is still well-defined.\nCoincident coordinates. A point whose k_d-th-neighbor distance is zero (r_k = 0) receives NaN log-density and NaN contrast. A point with no finite baseline neighbors also receives NaN contrast.\nper_dataset. When true, each dataset is built and evaluated on its own KDTree (no cross-dataset neighbors); when false, all emitters are pooled into one tree. Either way the per-emitter outputs are returned in original emitter order, and info.statistic is the median over the finite contrasts of the whole input.\nNaN handling. Both extras vectors are initialized to NaN and only filled where the feature is computable, so callers should mask with isfinite (as the example does) before thresholding or aggregating.","title":"Notes & caveats"},{"category":"section","location":"methods/voronoi/#Voronoi-(SR-Tesseler)","page":"Voronoi (SR-Tesseler)","text":"Density clustering of 2D SMLM localizations via Voronoi tessellation, following Levet et al., Nature Methods 2015. It is a cluster labeling backend selected by dispatch on a VoronoiConfig, returning a labeled SMLD and a ClusterInfo with algorithm = :voronoi.\n\n(Image: Voronoi density and SR-Tesseler clusters)\n\nLeft: per-emitter Voronoi density (bright = dense). Right: dense cells, grouped by Delaunay adjacency, form the clusters; sparse points (gray) are noise.","title":"Voronoi (SR-Tesseler)"},{"category":"section","location":"methods/voronoi/#Concept","page":"Voronoi (SR-Tesseler)","text":"Every localization is assigned a Voronoi cell — the region of the plane closer to that localization than to any other. A small cell means the point sits in a crowded neighborhood, so small cell area is a direct proxy for high local density. A point is called dense when its cell area falls below a threshold derived from the group mean, and dense points that touch along the Delaunay graph are agglomerated into clusters. There is no length scale to calibrate (no eps, no bandwidth): the only knob is a dimensionless density multiplier, which makes the method parameter-light and calibration-free.","title":"Concept"},{"category":"section","location":"methods/voronoi/#How-it-works","page":"Voronoi (SR-Tesseler)","text":"For a group of n localizations the backend builds the Delaunay triangulation of the coordinates and the dual Voronoi tessellation, clipped to the convex hull so every generator i has a finite cell of area A_i. The local density is the reciprocal of the cell area,\n\nrho_i = frac1A_i\n\nThe threshold is taken from the group's mean cell area,\n\nbarA = frac1n sum_i=1^n A_i\n\nand a point is classified as dense when its cell is smaller than the mean area divided by the density multiplier f (density_factor):\n\nA_i  fracbarAf\n\nEquivalently, in density terms, rho_i  f cdot barA^-1 — the local density must exceed f times the density corresponding to the mean area. Larger f demands a smaller cell (higher density) to qualify, so fewer points pass.\n\nClusters are then the connected components of the dense points over the Delaunay adjacency graph: starting from each unlabeled dense point, the search walks to Delaunay neighbors that are themselves dense (the ghost neighbor -1 is filtered), flood-filling one component at a time. Each raw component whose size is at least min_points becomes a cluster; smaller components are relabeled noise (id = 0). Surviving clusters are renumbered compactly 1..K within the group.","title":"How it works"},{"category":"section","location":"methods/voronoi/#Configuration","page":"Voronoi (SR-Tesseler)","text":"VoronoiConfig is a Base.@kwdef struct; every field has a default, so you only set what you want to change.\n\nfield default unit meaning\ndensity_factor 2.0 — density threshold multiplier f; a point is dense when its cell area < mean_area / density_factor. Higher → stronger density required → fewer dense points. Must be > 0\nmin_points 5 count minimum cluster size; Delaunay components smaller than this become noise. Must be ≥ 1\nuse_3d false bool must be false — 3D Voronoi clustering is not supported (see caveats)\nper_dataset true bool cluster within each dataset independently, so (dataset, id) uniquely identifies a cluster across a multi-dataset SMLD\nremove_unclustered false bool if true, drop noise emitters (id == 0) from the returned SMLD\n\nusing SMLMClustering\n\ncfg = VoronoiConfig(\n    density_factor     = 2.0,   # density threshold multiplier\n    min_points         = 5,     # minimum cluster size\n    per_dataset        = true,\n    remove_unclustered = false,\n)\n\n(smld_out, info) = cluster(smld, cfg)","title":"Configuration"},{"category":"section","location":"methods/voronoi/#Output-and-interpretation","page":"Voronoi (SR-Tesseler)","text":"cluster(smld, cfg) returns a tuple (smld_out, info):\n\nsmld_out — a fresh BasicSMLD (the input is not mutated; emitters are deep-copied). Each emitter's id carries its label: 0 for noise, 1..K for clusters. With remove_unclustered = true, noise emitters are dropped from smld_out entirely.\ninfo::ClusterInfo — run summary with algorithm = :voronoi. Useful fields: n_locs_in, n_clustered (emitters with id > 0), n_noise (id == 0), n_clusters, cluster_sizes (size of cluster k at cluster_sizes[k]), and elapsed_s.\n\n(smld_out, info) = cluster(smld, VoronoiConfig(density_factor = 2.0))\nprintln(\"$(info.n_clustered)/$(info.n_locs_in) clustered into $(info.n_clusters) clusters\")\n\nWhen per_dataset = true, labels are assigned independently per dataset, so a given id only identifies a unique cluster together with its dataset.","title":"Output & interpretation"},{"category":"section","location":"methods/voronoi/#Notes-and-caveats","page":"Voronoi (SR-Tesseler)","text":"2D only. Coordinates are taken from each emitter's (x, y) (in µm); the z-coordinate never participates. Setting use_3d = true raises an ArgumentError — DelaunayTriangulation.jl does not implement 3D Voronoi tessellation. For 3D data, use DBSCANConfig or HierarchicalConfig with use_3d = true.\nTiny groups become noise. A tessellation needs at least 3 non-collinear points, so any group with fewer than 3 localizations is tagged all-noise (id = 0) and contributes no cluster.\nExact duplicates are rejected. A group containing exact-duplicate (x, y) coordinate pairs raises an ArgumentError (coincident generators break cell-area lookup). Deduplicate input localizations before calling cluster.\nConvex-hull clipping bias. Cells are clipped to the convex hull of the generator set, so generators on the hull get cells smaller than their true infinite-plane area. This can bias the mean-area estimate on very small groups; for SMLM datasets with thousands of localizations the effect is second-order.\nScaling. Cluster membership is governed by the relative density threshold mean_area / density_factor, not an absolute length, so the result is essentially invariant to a uniform global scaling of the coordinates within a group (up to triangulation degeneracies); only the density contrast between points matters.","title":"Notes & caveats"},{"category":"section","location":"methods/voronoi/#References","page":"Voronoi (SR-Tesseler)","text":"Levet, F., Hosy, E., Kechkar, A., Butler, C., Beghin, A., Choquet, D. & Sibarita, J.-B. \"SR-Tesseler: a method to segment and quantify localization-based super-resolution microscopy data.\" Nature Methods 12, 1065–1071 (2015). doi:10.1038/nmeth.3579\nDelaunayTriangulation.jl — the pure-Julia (2D) Delaunay/Voronoi engine used to build the tessellation and adjacency graph.","title":"References"},{"category":"section","location":"methods/hopkins/#Hopkins-statistic","page":"Hopkins statistic","text":"A cluster_statistics backend, selected by HopkinsConfig, that measures the clustering tendency of an unlabeled point set — whether the data is clustered at all, before any cluster is actually formed. The statistic is sample-based and uses a KDTree nearest-neighbor backend (NearestNeighbors.jl).\n\n(Image: Hopkins H: clustered versus uniform)\n\nHopkins H on clustered data (H → 1) versus spatially-uniform data (H ≈ 0.5).","title":"Hopkins statistic"},{"category":"section","location":"methods/hopkins/#Concept","page":"Hopkins statistic","text":"Clustering tendency is a separate question from clustering itself. Algorithms like DBSCAN will partition any input into clusters and noise; the Hopkins statistic instead asks the prior question — is there structure worth clustering, or is the point set indistinguishable from spatial randomness?\n\nIt answers this by comparing two sets of nearest-neighbor distances: the NN distances of real data points to other real points, against the NN distances of uniformly-sampled synthetic points (drawn from the same bounding box) to the real data. When the data is clustered, real points sit close to their neighbors (small real-to-real distances) while a uniform point typically lands in empty space (large synthetic-to-data distances), driving the statistic toward 1. When the data is itself uniform, the two distance sets look alike and the statistic sits near 0.5.","title":"Concept"},{"category":"section","location":"methods/hopkins/#How-it-works","page":"Hopkins statistic","text":"For each repeat, with m = n_samples and the data living in d dimensions (d = 2, or d = 3 when use_3d = true):\n\nSynthetic reference points. Draw m points uniformly in the per-group axis-aligned bounding box. For each reference point, let u_i be its nearest-neighbor distance to the real data (a k = 1 KDTree query).\nSampled real points. Draw m real points without replacement from the data set. For each sampled point, let w_i be its nearest-neighbor distance to the other real points, excluding itself (a k = 2 KDTree query, taking the second-nearest, since the nearest neighbor of a data point in its own tree is itself at distance 0).\n\nThe Hopkins statistic for that repeat raises each distance to the power d (the spatial dimension) and forms:\n\nH = fracsum_i=1^m u_i^d\n         sum_i=1^m u_i^d + sum_i=1^m w_i^d\n\nWhen the denominator sum u_i^d + sum w_i^d is exactly 0, that repeat's H is NaN. The reported value is the mean of H over the random_repeats independent repeats.","title":"How it works"},{"category":"section","location":"methods/hopkins/#Configuration","page":"Hopkins statistic","text":"HopkinsConfig <: AbstractStatisticsConfig:\n\nfield default unit meaning\nn_samples 20 — number of reference / sampled points m per repeat; if it exceeds a group's point count, that group returns NaN (not an error)\nrandom_repeats 1 — number of independent repeats to average; higher reduces variance at linear cost\nseed nothing — when set (Int), seeds an internal Xoshiro for reproducibility; when nothing, uses the global RNG\nuse_3d false — include the z-coordinate and use d = 3 in the formula\nper_dataset true — when true, compute Hopkins per dataset and report the across-dataset mean as statistic; when false, pool all emitters into a single H\nregion nothing — observation window for the uniform reference points (2D). nothing = data bounding box; a polygon Vector{NTuple{2,Float64}} = rejection-sample references inside it; :metadata = read smld.metadata[\"edge_outer_polygon\"] (written by classify_emitters); Dict(dataset_id => polygon) = per dataset. Incompatible with use_3d = true\n\nValidated at dispatch entry: n_samples ≥ 1 and random_repeats ≥ 1, else an ArgumentError is raised.\n\nusing SMLMClustering\n\ncfg = HopkinsConfig(\n    n_samples      = 50,    # reference / sampled point count per repeat\n    random_repeats = 10,    # average over independent repeats\n    seed           = 1,     # RNG seed for reproducibility\n    use_3d         = false,\n    per_dataset    = true,  # per-dataset H in extras + across-dataset mean as statistic\n)\n\n(_, info) = cluster_statistics(smld, cfg)\n\nprintln(\"Hopkins H = \", round(info.statistic, digits = 3))\nper_ds = info.extras[:hopkins_per_dataset]   # Vector{Float64}, one H per dataset\nprintln(\"per-dataset H = \", per_ds)","title":"Configuration"},{"category":"section","location":"methods/hopkins/#Output-and-interpretation","page":"Hopkins statistic","text":"cluster_statistics returns (smld, info::ClusterStatisticsInfo). The SMLD is passed through unchanged — the Hopkins backend reads coordinates only and writes nothing back.\n\ninfo.statistic — the Hopkins H. When per_dataset = true this is the mean over datasets with a non-NaN result (NaN only if every dataset is NaN); when per_dataset = false it is the single pooled H.\ninfo.statistic_name and info.algorithm are both :hopkins.\ninfo.extras[:hopkins_per_dataset] — a Vector{Float64} holding the per-dataset H (one entry per group, in dataset order). This key is present only when per_dataset = true; with per_dataset = false the extras dictionary is empty.\n\nReading the value:\n\nH meaning\napprox 05 data is statistically indistinguishable from uniform spatial randomness (Poisson)\nto 10 strong clustering tendency\nto 00 regular / lattice-like (anti-clustering, even spacing)\n\nEdge cases return NaN for the affected group rather than erroring:\n\nan empty group (no emitters in that dataset),\nn_samples > n_points for the group (or fewer than 2 points),\na zero-extent bounding box — all points coincident along some axis, so uniform sampling is degenerate.\n\nWith per_dataset = true, a NaN group is recorded in extras[:hopkins_per_dataset] and excluded from the statistic mean.","title":"Output & interpretation"},{"category":"section","location":"methods/hopkins/#Notes-and-caveats","page":"Hopkins statistic","text":"Observation window (the region field). Hopkins is window-sensitive — its null is \"uniform within a stated window\", and the reference points define that window. The default window is the data bounding box, so data that is uniform but confined to a non-convex boundary (e.g. a cell) reads as falsely clustered: bbox references fall in the empty corners, far from any data, inflating H. Pass a region polygon (or :metadata to use EdgeClassify's edge_outer_polygon) to sample references inside the actual domain and recover the correct null.\nSampling variance. H is a Monte-Carlo estimate: each call draws random reference and sample points, so successive runs differ. Raise random_repeats to average the variance down (linear cost), and set seed to make a run bit-for-bit reproducible.\nn_samples relative to group size. n_samples must not exceed the number of points in a group; oversized requests yield NaN for that group. Smaller m gives a noisier estimate, so pair small samples with more repeats.\n2D / 3D. The default is 2D (d = 2); set use_3d = true to include the z-coordinate and use d = 3 in both the KDTree queries and the distance power.","title":"Notes & caveats"},{"category":"section","location":"methods/hopkins/#References","page":"Hopkins statistic","text":"Hopkins, B. and Skellam, J. G. (1954). \"A new method for determining the type of distribution of plant individuals.\" Annals of Botany, 18(2), 213–227.","title":"References"},{"category":"section","location":"methods/dbscan/#DBSCAN","page":"DBSCAN","text":"DBSCAN (Density-Based Spatial Clustering of Applications with Noise) labels each SMLM localization as belonging to a cluster or as noise based on local point density. It is a cluster labeling backend selected by passing a DBSCANConfig, and is backed by Clustering.dbscan from Clustering.jl. The call cluster(smld, cfg::DBSCANConfig) deep-copies the input emitters, writes a per-emitter label into emitter.id (0 = noise, 1..K = cluster), and returns (smld_out, ClusterInfo).\n\n(Image: DBSCAN under two eps values)\n\nThe same field labeled with eps_nm too tight (left — the top-right cluster fragments and more points fall to noise) vs. well-chosen (right). Noise (id = 0) is gray.","title":"DBSCAN"},{"category":"section","location":"methods/dbscan/#Concept","page":"DBSCAN","text":"DBSCAN groups points that lie in dense neighborhoods and discards sparse points as noise. It needs two parameters: a neighborhood radius eps_nm (ε) and a count min_points. A point is a core point when at least min_points localizations fall within ε of it. Core points that are within ε of one another are stitched together into the same cluster; non-core points that sit within ε of a core point are pulled in as border points; everything else is noise. DBSCAN does not require the number of clusters up front, finds arbitrarily shaped clusters, and labels low-density localizations as noise rather than forcing them into a cluster.\n\nThis backend is the package's recommended default for general clustering: it scales to large datasets (KD-tree neighbor queries, no O(n²) pairwise-distance matrix), and it works in both 2D and 3D.","title":"Concept"},{"category":"section","location":"methods/dbscan/#How-it-works","page":"DBSCAN","text":"Let P be the set of localization coordinates in a group and mathrmdist(pq) the Euclidean distance between two points. With radius varepsilon and threshold m = textttmin_points:\n\nThe ε-neighborhood of a point p is\n\nN_varepsilon(p) =  q in P  mathrmdist(p q) le varepsilon \n\nA point p is a core point iff its ε-neighborhood is large enough:\n\nN_varepsilon(p) ge m\n\nA point q is directly density-reachable from a core point p when q in N_varepsilon(p). A point is density-reachable from p if it is connected to p through a chain p = p_1 p_2 dots p_k = q in which each p_i+1 is directly density-reachable from the core point p_i. Two points are density-connected if both are density-reachable from a common core point. A cluster is then a maximal set of mutually density-connected points — the connected core points plus their reachable border points. Points that are neither core nor reachable from any core point are noise.","title":"How it works"},{"category":"section","location":"methods/dbscan/#Units-and-the-metric-the-code-uses","page":"DBSCAN","text":"Emitter coordinates on AbstractEmitter subtypes are stored in microns, but eps_nm is given in nanometers. The backend converts once, up front:\n\nvarepsilon_mu m = fractexttteps_nm1000\n\nClustering is then performed directly on the micron coordinate matrix, so the neighborhood test is the Euclidean distance le varepsilon_mu m in micron space. Neighbor queries use a KD-tree (the Clustering.dbscan default), which is what gives the backend its sub-quadratic scaling.","title":"Units and the metric the code uses"},{"category":"section","location":"methods/dbscan/#2D-vs-3D","page":"DBSCAN","text":"The coordinate matrix is built from (x, y) when use_3d = false and from (x, y, z) when use_3d = true. In 3D the same scalar radius varepsilon_mu m is applied isotropically across all three axes (the neighborhood is a sphere, not an ellipsoid). use_3d = true requires 3D emitters (e.g. Emitter3DFit); a 2D emitter type raises an error because it has no z field.","title":"2D vs 3D"},{"category":"section","location":"methods/dbscan/#Configuration","page":"DBSCAN","text":"DBSCANConfig <: AbstractClusterConfig. Construct it with keywords; eps_nm is required, the rest take the defaults below.\n\nfield default unit meaning\neps_nm (required) nm neighborhood radius ε; converted to microns internally (varepsilon_mu m = texttteps_nm1000). Must be > 0.\nmin_points 5 count minimum points in an ε-neighborhood for a point to be a core point (classical DBSCAN minPts); also the minimum cluster size. Must be ≥ 1.\nuse_3d false — cluster in (x, y, z) when true (requires 3D emitters), otherwise in (x, y).\nper_dataset true — when true, cluster within each dataset index independently so (dataset, id) uniquely identifies a cluster in a multi-dataset SMLD; when false, all emitters are clustered together and id alone identifies the cluster.\nremove_unclustered false — when true, emitters tagged as noise (id == 0) are dropped from the returned SMLD.\n\nThe last four fields (min_points, use_3d, per_dataset, remove_unclustered) are the shared fields carried by every backend config; only eps_nm is specific to DBSCAN.\n\nusing SMLMClustering\n\n# `smld::SMLMData.BasicSMLD` is your localization set (coordinates in microns).\ncfg = DBSCANConfig(\n    eps_nm             = 50.0,   # neighborhood radius in nm (required)\n    min_points         = 5,      # core-point threshold / minimum cluster size\n    use_3d             = false,  # set true for (x, y, z) on 3D emitters\n    per_dataset        = true,   # cluster each dataset independently\n    remove_unclustered = false,  # keep noise (id == 0) in the output\n)\n\nsmld_out, info = cluster(smld, cfg)\n\nprintln(info)                                  # ClusterInfo summary\nprintln(\"clustered \", info.n_clustered, \"/\", info.n_locs_in,\n        \" into \", info.n_clusters, \" clusters; \", info.n_noise, \" noise\")\n\n# Per-emitter labels live on emitter.id (0 = noise, 1..K = cluster id).\nlabels = [e.id for e in smld_out.emitters]","title":"Configuration"},{"category":"section","location":"methods/dbscan/#Output-and-interpretation","page":"DBSCAN","text":"cluster returns a 2-tuple (smld_out, info):\n\nsmld_out::BasicSMLD — a deep copy of the input with cluster labels written onto each emitter.id. The input smld is never modified. id == 0 marks noise; id ∈ 1..K marks the cluster the emitter belongs to. When per_dataset = true, ids are local to each dataset, so the pair (dataset, id) is what uniquely identifies a cluster across the SMLD. When remove_unclustered = true, noise emitters are dropped and only clustered emitters remain.\ninfo::ClusterInfo — the run summary, with algorithm = :dbscan:\n\nfield type meaning\nn_locs_in Int number of input localizations\nn_clustered Int localizations assigned to a cluster (id > 0)\nn_noise Int localizations tagged as noise (id == 0); equals n_locs_in - n_clustered\nn_clusters Int number of distinct clusters formed\ncluster_sizes Vector{Int} size of each cluster, indexed by cluster id (cluster_sizes[k] is the size of cluster k); length == n_clusters\nalgorithm Symbol :dbscan\nelapsed_s Float64 wall-clock time of the cluster call, in seconds\n\nRead n_noise as the count of localizations DBSCAN judged too sparse to belong to any cluster (raise eps_nm or lower min_points to recover more of them). Read cluster_sizes as the per-cluster member counts in cluster-id order; with per_dataset = true it concatenates the surviving clusters across datasets in sorted-dataset order, so its length is the total cluster count, not a per-dataset count.","title":"Output & interpretation"},{"category":"section","location":"methods/dbscan/#Notes-and-caveats","page":"DBSCAN","text":"Scaling. Neighbor queries run on a KD-tree, so there is no O(n²) pairwise-distance matrix and no corresponding memory blow-up. This is the reason DBSCAN is preferred over the hierarchical backend for groups with many thousands of localizations.\nmin_points semantics. The same value sets both the core-point threshold and the minimum cluster size: a candidate cluster whose final membership is below min_points is dropped (its points become noise). min_points must be ≥ 1; eps_nm must be > 0. Both are validated at the start of the call and raise ArgumentError otherwise.\nBoundary points / counting source of truth. Sizes and labels are recounted from Clustering.jl's per-point assignments (last-writer-wins, each point assigned exactly once), not from its counts. A boundary point that touches two clusters is counted in both clusters' counts, so summing counts can exceed the point count and inflate sizes; the backend avoids this by using assignments, then compact-relabels the survivors 1..K within each group so the reported cluster_sizes stay consistent with the written labels.\n2D vs 3D. Both are supported. 3D needs emitters that carry a z field; otherwise an error is raised.\nPer-dataset grouping. With per_dataset = true, datasets are processed in sorted index order and cluster ids restart from 1 in each dataset. Empty groups are skipped.\nDuplicates. DBSCAN imposes no exact-duplicate-coordinate guard; coincident (x, y) (or (x, y, z)) localizations are clustered normally. This is unlike the Voronoi/MRF backends, which raise ArgumentError on duplicate generators.","title":"Notes & caveats"},{"category":"section","location":"methods/dbscan/#References","page":"DBSCAN","text":"M. Ester, H.-P. Kriegel, J. Sander, and X. Xu, \"A Density-Based Algorithm for Discovering Clusters in Large Spatial Databases with Noise,\" Proceedings of the 2nd International Conference on Knowledge Discovery and Data Mining (KDD-96), AAAI Press, 1996, pp. 226–231.\nImplementation backend: Clustering.jl (Clustering.dbscan).","title":"References"},{"category":"section","location":"methods/point_hysteresis/#Point-hysteresis","page":"Point hysteresis","text":"A cluster labeling backend selected by PointHysteresisConfig. It performs seed-and-grow connected-component labeling on a k-nearest-neighbour point graph: high-confidence seed points anchor components, the traversal grows through lower-confidence support points, and any component without a seed — or smaller than min_points — is dropped to noise. The two confidence levels (seed and support) are supplied as per-emitter boolean masks; they are the \"strong\" and \"weak\" arms of a density hysteresis computed outside the backend (typically from LocalContrastFeature).\n\n(Image: Seed → support → grown clusters)\n\nHysteresis seed-and-grow: a few high-confidence seed points (left) grow through the lower-threshold support set (middle) into the final clusters (right). Seed and support masks here come from LocalContrastFeature.","title":"Point hysteresis"},{"category":"section","location":"methods/point_hysteresis/#Concept","page":"Point hysteresis","text":"A single density cutoff forces one compromise everywhere: set it high and you fragment real clusters wherever the density dips; set it low and you bleed noise into the foreground. Hysteresis uses two cutoffs instead of one. A strong (seed) threshold marks only the points you are confident belong to a structure; a weaker (support) threshold marks every plausible foreground point. Clusters are then seeded at the strong points and grown outward into adjacent support points, stopping where the support mask stops.\n\nThis reduces both failure modes at once:\n\nUnder-segmentation is curbed because a component is only emitted if it actually contains seed evidence — a diffuse blob of weak points with no strong core is discarded rather than merged in.\nOver-segmentation is curbed because growth crosses the weaker support threshold, so a real cluster whose periphery thins out is still connected through its support shell instead of splintering at the seed cutoff.\n\nBecause the strong evidence only gates a component (it does not flood from a bad emission model), a poorly calibrated mask yields fewer seeds — not a runaway merge. This is the explicit, discrete counterpart to the GMM+Potts MRF backend (MRFDensityClusterConfig), where the density/prior coupling is implicit and iterative.","title":"Concept"},{"category":"section","location":"methods/point_hysteresis/#How-it-works","page":"Point hysteresis","text":"The backend is feature-agnostic: it consumes two boolean masks and a graph parameter. The masks are produced upstream by thresholding a density feature. The reference feature is LocalContrastFeature, which gives each emitter a fine-scale kNN log-density and a local-contrast value:\n\nf_i = logleft(frack_dpi r_k_d^2right)\nqquad\nc_i = f_i - operatornamemedian_j in mathcalN_k_b(i) f_j \n\nwhere r_k_d is the distance to emitter i's k_d-th neighbour and mathcalN_k_b(i) is its k_b-nearest-neighbour set. The caller turns these into the two masks with a strong and a weak threshold, e.g. seeds at high contrast and support at a looser contrast, both above a density floor. The seed mask must be a subset of the support mask (every seed is also a support point); a violation raises ArgumentError.\n\nGiven masks S (seed) and U (support) with S subseteq U, the backend builds a directed kNN graph over the emitter coordinates and labels the support subgraph:\n\nGraph. Build a KDTree over the coordinates (2D (x, y), or (x, y, z) when use_3d). For the current group of n points, use degree k = min(textttgraph_k n-1). There is a directed edge i to j when j is among the k nearest neighbours of i (self excluded). The graph is asymmetric: i to j does not imply j to i.\nSeed-and-grow. Iterate emitters in input order. At each unvisited support point, start a stack-based graph traversal (a flood) that follows kNN edges but only ever steps onto support points:\nC =  v  v text reachable from the start through support points along  ito j text edges \nEvery reached node is marked visited, so each support point lands in exactly one component. Points outside U are never entered and act as barriers.\nKeep rule. After a component C is closed, it becomes a cluster iff it contains seed evidence and is large enough:\ntextkeep  C iff bigl(C cap S neq varnothingbigr) wedge bigl(lvert Crvert ge textttmin_pointsbigr)\nKept components receive a fresh cluster id (1 2 dots within the group); everything else — support components with no seed, undersized components, and all non-support points — stays at id = 0 (noise).\n\nThe traversal stops when no support point reachable from the seeded region remains unvisited. Note the asymmetric-kNN consequence: with graph_k much smaller than the cluster size, a true-but-outlying support point may have no incoming edge from the component and remain unreachable; raising graph_k makes the graph denser and the growth more inclusive.","title":"How it works"},{"category":"section","location":"methods/point_hysteresis/#Configuration","page":"Point hysteresis","text":"PointHysteresisConfig holds only the reusable knobs; the per-cell seed and support masks are required keyword arguments to cluster, not config fields (the same config instance is meant to be reused across many SMLDs).\n\nfield default unit meaning\ngraph_k 12 — degree of the kNN graph used for the grow traversal (clamped to n-1 per group)\nmin_points 100 localizations minimum component size for a cluster to be kept; smaller components → noise\nuse_3d false — build the kNN graph in (x, y, z); requires 3D emitters (e.g. Emitter3DFit)\nper_dataset false — cluster within each dataset independently; ids are local, so (dataset, id) is unique\nremove_unclustered false — drop emitters with id = 0 from the returned SMLD\n\nRequired keyword arguments to cluster(smld, cfg; seed, support):\n\nkeyword type meaning\nseed AbstractVector{Bool} high-confidence foreground; length must equal length(smld.emitters). A component must contain ≥1 to be kept\nsupport AbstractVector{Bool} candidate foreground (same length). Growth only crosses support points. Must be a superset of seed\n\nValidation at dispatch entry: seed/support lengths must match the emitter count, graph_k ≥ 1, min_points ≥ 1, and seed[i] ⟹ support[i] for every emitter — each violation raises ArgumentError.\n\nusing SMLMClustering\nusing Statistics   # for `quantile`\n\n# 1. Per-cell density feature → fine log-density + local contrast.\n(_, info_f) = cluster_statistics(smld,\n    LocalContrastFeature(density_k = 200, background_k = 2000))\ncontrast = info_f.extras[:contrast_per_emitter]\nfine     = info_f.extras[:log_density_per_emitter]\n\n# 2. Two thresholds on the same feature → the hysteresis arms.\nfine_floor = quantile(filter(isfinite, fine), 0.35)\nseed    = isfinite.(contrast) .& isfinite.(fine) .& (contrast .>  0.25) .& (fine .> fine_floor)\nsupport = isfinite.(contrast) .& isfinite.(fine) .& (contrast .> -0.05) .& (fine .> fine_floor)\n\n# 3. Seed-and-grow on the kNN graph.\ncfg = PointHysteresisConfig(graph_k = 12, min_points = 150)\n(smld_out, info) = cluster(smld, cfg; seed = seed, support = support)\n\nprintln(info)  # ClusterInfo(.../... clustered, K clusters, algorithm=:point_hysteresis, ... ms)","title":"Configuration"},{"category":"section","location":"methods/point_hysteresis/#Output-and-interpretation","page":"Point hysteresis","text":"cluster returns (smld_out, info::ClusterInfo).\n\nsmld_out is a deep copy of the input SMLD; the input is never mutated. All input ids are zeroed before traversal (prior labels never leak), and cluster labels are written onto the copy's emitter.id: 0 = noise, 1..K = clusters. With remove_unclustered = true, id = 0 emitters are dropped from smld_out; otherwise every input emitter is present with its assigned id.\ninfo::ClusterInfo carries:\nfield meaning\nn_locs_in input localization count\nn_clustered emitters assigned to a cluster (id > 0)\nn_noise emitters left as noise (n_locs_in - n_clustered)\nn_clusters number of clusters formed\ncluster_sizes size of each kept component, in the order components were emitted\nalgorithm the Symbol :point_hysteresis\nelapsed_s wall-clock time of the cluster call, in seconds\n\nThere is no extras field on ClusterInfo; the masks and any density feature remain the caller's to keep. With per_dataset = false (default, a single pooled group) cluster ids run 1..K and cluster_sizes[k] is the size of cluster k. With per_dataset = true, ids restart at 1 within each dataset (so (dataset, id) is the unique key) while cluster_sizes is appended across groups in iteration order — its index no longer equals a global cluster id.","title":"Output & interpretation"},{"category":"section","location":"methods/point_hysteresis/#Notes-and-caveats","page":"Point hysteresis","text":"2D and 3D. Both are supported via use_3d. With use_3d = false the graph is built on (x, y); with use_3d = true it is built on (x, y, z) and the emitters must expose a z property (e.g. Emitter3DFit), otherwise coordinate extraction errors out.\nCoordinates / scaling. Distances are computed directly in the emitters' stored coordinate units (µm) with no per-axis scaling, so x, y (and z) must be on a common physical scale for the kNN graph to be meaningful.\nmin_points. Applied twice: a whole group with n < min_points is skipped before any traversal, and each individual component with |C| < min_points is dropped to noise. It is the same threshold as the shared min_points contract across the clustering backends.\nGroup skip. A group is also skipped when min(graph_k, n-1) < 1 (i.e. n ≤ 1), so a degenerate group never forms a cluster.\nAsymmetric-kNN reachability. Because edges are directed (current node's kNN), tight clusters with graph_k ≪ N can leave peripheral support points unreachable; prefer a larger graph_k at small N if growth looks clipped.\nMask provenance. The hysteresis quality is entirely in how seed and support are derived; this backend only enforces seed ⊆ support and the graph/keep rules. Choosing the feature and the two thresholds is the caller's responsibility (see LocalContrastFeature for the reference recipe).","title":"Notes & caveats"},{"category":"section","location":"guide/#User-Guide","page":"User Guide","text":"This guide covers the calling conventions shared by every backend: the inputs, the three verbs and what they return, the configuration fields common to all labeling backends, and how to sanity-check a result. For the algorithms themselves see the Methods overview.","title":"User Guide"},{"category":"section","location":"guide/#Inputs-and-units","page":"User Guide","text":"Every verb takes an SMLMData.BasicSMLD as its first argument. Emitter coordinates are in µm. Parameters that express a physical length are given in nm (e.g. eps_nm, cut_threshold for distance linkages) and converted internally; each backend page states the unit of every field.\n\nAn SMLD may hold multiple datasets (cells, ROIs, acquisitions). By default each backend processes datasets independently — see per_dataset below.","title":"Inputs and units"},{"category":"section","location":"guide/#The-three-verbs","page":"User Guide","text":"The three verbs share one calling convention — verb(smld, cfg) → (smld, info), dispatched on the concrete config type — but do different things with the result. cluster writes an integer instance label onto emitter.id; classify_emitters leaves emitter.id alone and stores a fixed semantic class in metadata; cluster_statistics writes nothing. Because labeling and classification use different fields, they compose (classify → filter → cluster, or cluster → inspect by region) — see Labeling vs. classification in the Methods overview.","title":"The three verbs"},{"category":"section","location":"guide/#cluster-—-labeling","page":"User Guide","text":"smld_out, info = cluster(smld, cfg)\n\ncluster is non-mutating: the input emitters are deep-copied, cluster labels are written onto the copy's emitter.id (0 = noise, 1..K = clusters), and info::ClusterInfo carries the summary. When cfg.remove_unclustered = true the returned smld_out contains only clustered emitters.\n\nClusterInfo fields:\n\nField Type Meaning\nn_locs_in Int input localization count\nn_clustered Int localizations assigned to a cluster (id > 0)\nn_noise Int noise localizations (id == 0)\nn_clusters Int number of distinct clusters\ncluster_sizes Vector{Int} size of each cluster, indexed by cluster id\nalgorithm Symbol :dbscan, :voronoi, :hierarchical, …\nelapsed_s Float64 wall-clock time of the cluster call (s)","title":"cluster — labeling"},{"category":"section","location":"guide/#cluster_statistics-—-read-only-statistics","page":"User Guide","text":"smld, info = cluster_statistics(smld, stats_cfg)\n\nPass-through: the first return value is the same SMLD reference as the input (no allocation, no mutation); the two-tuple shape is kept for ecosystem symmetry. info::ClusterStatisticsInfo carries:\n\nField Type Meaning\nn_locs_in Int input localization count\nstatistic Float64 primary scalar result (e.g. Hopkins H)\nstatistic_name Symbol identifier for statistic\nalgorithm Symbol backend identifier\nelapsed_s Float64 wall-clock time (s)\nextras Dict{Symbol,Any} per-backend supplementary outputs (vectors, per-group breakdowns)\n\nConvention for vector-valued backends: a meaningful summary scalar (mean, median, …) goes in statistic; the full per-emitter / per-group vector goes in extras under a descriptive key. This keeps info.statistic ergonomic while preserving the full result.","title":"cluster_statistics — read-only statistics"},{"category":"section","location":"guide/#classify_emitters-—-edge-/-membrane-/-interior","page":"User Guide","text":"smld, info = classify_emitters(smld, cfg)   # cfg :: AbstractEdgeClassifyConfig\n\nReturns a new SMLD with the published mask threaded into smld.metadata[\"edge_cells\"] / [\"edge_outer_polygon\"]; the per-emitter class lives in info::EdgeClassifyInfo (class::Vector{Symbol}), read via in_cell / interior_mask. See Edge / Membrane Classification.","title":"classify_emitters — edge / membrane / interior"},{"category":"section","location":"guide/#Shared-configuration-fields","page":"User Guide","text":"Every labeling backend config carries these fields with the same defaults:\n\nField Default Meaning\nmin_points 5 minimum points for a valid cluster\nuse_3d false include the z-coordinate\nper_dataset true cluster within each dataset independently\nremove_unclustered false drop noise emitters from the returned SMLD\n\nWhen per_dataset = true, (dataset, id) uniquely identifies a cluster across a multi-dataset SMLD; ids are local to each dataset.\n\nnote: Backend-specific defaults\nA few backends override these defaults where the algorithm warrants it — e.g. Point hysteresis defaults to min_points = 100 and per_dataset = false, and HDBSCAN reinterprets min_points as the core-distance k. Each method page lists its own defaults.","title":"Shared configuration fields"},{"category":"section","location":"guide/#Is-the-result-sane?","page":"User Guide","text":"A quick checklist after a cluster run:\n\nNoise fraction. info.n_noise / info.n_locs_in near 1.0 means the length scale is too tight (or the data really is unclustered — confirm with the Hopkins statistic); near 0.0 with few, huge clusters means it is too loose.\nCluster-size distribution. info.cluster_sizes should not be dominated by a single giant cluster that swallowed the field (a classic single-ε failure — see MRF density-regime).\nCount vs. expectation. info.n_clusters should be the right order of magnitude for the structure you expect.\n3D. Several backends are 2D only and raise ArgumentError on use_3d = true; check the method page before enabling it.","title":"Is the result sane?"},{"category":"section","location":"guide/#Large-datasets","page":"User Guide","text":"DBSCAN, Voronoi (SR-Tesseler) and Point hysteresis avoid the O(n²) distance matrix and are the right choice for ≫10,000 localizations per group.\nHierarchical builds a dense pairwise distance matrix per group — prefer it only for small groups.\nWith per_dataset = true, per-group cost is what matters, not the global n.","title":"Large datasets"},{"category":"section","location":"api/#API-Reference","page":"API Reference","text":"Complete reference for the exported API. The user-facing verbs and configuration types lead; diagnostics and lower-level helpers follow under Advanced & diagnostics.\n\n","title":"API Reference"},{"category":"section","location":"api/#Core-—-labeling","page":"API Reference","text":"","title":"Core — labeling"},{"category":"section","location":"api/#Spatial-statistics","page":"API Reference","text":"","title":"Spatial statistics"},{"category":"section","location":"api/#Edge-classification","page":"API Reference","text":"","title":"Edge classification"},{"category":"section","location":"api/#Multi-cell-mask","page":"API Reference","text":"","title":"Multi-cell mask"},{"category":"section","location":"api/#Edge-mask-report-and-figures","page":"API Reference","text":"compute_edge_report / write_edge_report / class_codes are core (no plotting deps). plot_edge_report and render_classes are provided by SMLMClusteringFiguresExt and require both CairoMakie and SMLMRender to be loaded.","title":"Edge-mask report & figures"},{"category":"section","location":"api/#Advanced-and-diagnostics","page":"API Reference","text":"","title":"Advanced & diagnostics"},{"category":"function","location":"api/#SMLMClustering.cluster","page":"API Reference","text":"cluster(smld::SMLMData.BasicSMLD, cfg::AbstractClusterConfig) -> (smld_out, ClusterInfo)\n\nCluster the localizations in smld using the backend selected by the concrete type of cfg. The input smld is not modified — each backend deep-copies the input emitters and writes cluster labels onto the copy's emitter.id (0 marks noise, 1..K mark distinct clusters). If cfg.remove_unclustered is true the returned SMLD contains only clustered emitters.\n\nDispatch on AbstractClusterConfig alone has no implementation — concrete backends supply a method specialized on their own config type. Calling cluster with an unsupported config raises a clear error.\n\nSee also: AbstractClusterConfig, ClusterInfo.\n\n\n\n\n\ncluster(smld::BasicSMLD, cfg::PointHysteresisConfig;\n        seed::AbstractVector{Bool}, support::AbstractVector{Bool})\n    -> (smld_out, ClusterInfo)\n\nRun point-graph hysteresis on smld using cfg's knobs and the per-cell seed and support boolean masks.\n\nRequired keyword arguments\n\nseed: high-confidence foreground points (length must equal length(smld.emitters)). Components must contain at least one to be kept.\nsupport: candidate foreground points (same length). BFS only crosses through support points. seed must imply support (every seed point is also a support point); violations raise ArgumentError.\n\nAlgorithm\n\nBuild a KDTree over the (per-dataset or pooled) emitter coordinates.\nIterate emitters in input order. Whenever an unvisited support point is found, BFS through the support set following kNN edges (graph_k neighbors per node, excluding self). Mark every reached node visited.\nAfter the BFS, if the component contains at least one seed point AND has at least cfg.min_points members, assign it a fresh cluster id (local within the per-dataset group, V3). Otherwise leave its members at id = 0 (the \"noise\" contract from AbstractClusterConfig — input ids are zeroed on the deepcopy before BFS so prior labels never leak).\n\nConvention\n\nThe config is non-mutating: emitters are deep-copied (V9) and ids are zeroed on the copy before BFS, so unclustered emitters come out as id = 0 regardless of input id values.\nAsymmetric kNN BFS: the BFS only crosses edges where the current node has the neighbor in its k-NN. For tight clusters with graph_k << N, outlier points may be unreachable; this is correct behavior matching the reference implementation, but at small N the user should prefer larger graph_k (or use full-mesh min(N-1, ...)).\n\nSee also: PointHysteresisConfig, LocalContrastFeature.\n\n\n\n\n\n","title":"SMLMClustering.cluster"},{"category":"type","location":"api/#SMLMClustering.AbstractClusterConfig","page":"API Reference","text":"AbstractClusterConfig <: SMLMData.AbstractSMLMConfig\n\nAbstract supertype for SMLMClustering backend configurations.\n\nEach backend defines a concrete subtype (e.g. DBSCANConfig, HDBSCANConfig, VoronoiConfig, HierarchicalConfig) and adds a cluster(smld, cfg) method specialized on it.\n\nShared fields (expected on every concrete subtype)\n\nmin_points::Int: minimum points for a valid cluster\nuse_3d::Bool: whether the z-coordinate participates in clustering\nper_dataset::Bool: if true, cluster within each dataset independently so that (dataset, id) uniquely identifies a cluster across a multi-dataset SMLD\nremove_unclustered::Bool: if true, drop emitters with id == 0 from the returned SMLD (noise/rejected localizations)\n\nAlgorithm-specific fields (eps_nm, min_cluster_size, density_factor, cut_nm, ...) live on the concrete subtype that needs them.\n\n\n\n\n\n","title":"SMLMClustering.AbstractClusterConfig"},{"category":"type","location":"api/#SMLMClustering.ClusterInfo","page":"API Reference","text":"ClusterInfo <: SMLMData.AbstractSMLMInfo\n\nSecondary output from cluster() — summary of a clustering run.\n\nFields\n\nn_locs_in::Int: number of input localizations\nn_clustered::Int: number of localizations assigned to a cluster (id > 0)\nn_noise::Int: number of localizations tagged as noise (id == 0)\nn_clusters::Int: number of distinct clusters formed\ncluster_sizes::Vector{Int}: size of each cluster, indexed by cluster id (cluster_sizes[k] is the size of cluster k); length equals n_clusters\nalgorithm::Symbol: backend identifier (:dbscan, :hdbscan, :voronoi, :hierarchical)\nelapsed_s::Float64: wall-clock time spent in the cluster call, in seconds\n\nExample\n\n(smld_out, info) = cluster(smld, DBSCANConfig(eps_nm=50.0, min_points=5))\nprintln(\"$(info.n_clustered)/$(info.n_locs_in) clustered into $(info.n_clusters) clusters\")\n\n\n\n\n\n","title":"SMLMClustering.ClusterInfo"},{"category":"type","location":"api/#SMLMClustering.DBSCANConfig","page":"API Reference","text":"DBSCANConfig(; eps_nm, min_points=5, use_3d=false, per_dataset=true, remove_unclustered=false)\n\nConfiguration for DBSCAN clustering of SMLM localizations.\n\nFields\n\neps_nm::Float64: neighborhood radius in nanometers. Coordinates on AbstractEmitter subtypes are in microns; the backend converts internally.\nmin_points::Int = 5: minimum number of points in an ε-neighborhood for a point to be a core point (classical DBSCAN minPts). Also used as the minimum cluster size.\nuse_3d::Bool = false: if true, cluster in (x, y, z); requires Emitter3DFit emitters. Otherwise cluster in (x, y).\nper_dataset::Bool = true: if true, cluster within each dataset index independently so that (dataset, id) uniquely identifies a cluster in a multi-dataset SMLD. If false, all emitters are clustered together and id alone identifies the cluster.\nremove_unclustered::Bool = false: if true, emitters tagged as noise (id == 0) are dropped from the returned SMLD.\n\nExample\n\ncfg = DBSCANConfig(eps_nm=50.0, min_points=5)\n(smld_out, info) = cluster(smld, cfg)\n\nSee also: AbstractClusterConfig, ClusterInfo, cluster.\n\n\n\n\n\n","title":"SMLMClustering.DBSCANConfig"},{"category":"type","location":"api/#SMLMClustering.HDBSCANConfig","page":"API Reference","text":"HDBSCANConfig(; min_points=5, min_cluster_size=nothing, knn_graph_k=30,\n                cluster_selection_method=:eom, allow_single_cluster=false,\n                halo_trim_frac=0.10, use_3d=false, per_dataset=true,\n                remove_unclustered=false)\n\nConfiguration for HDBSCAN clustering of SMLM localizations. Pure-Julia implementation of Campello/Moulavi/Sander 2013.\n\nFields\n\nmin_points::Int = 5: k for the core-distance computation. Larger values produce more conservative clusters.\nmin_cluster_size::Union{Int,Nothing} = nothing: minimum cluster size in the condensed tree. When nothing, defaults to min_points.\nknn_graph_k::Int = 30: width k' of the sparse k'-NN graph used as the MST scaffold. Must be large enough that the resulting MST is connected; the backend errors out (with a clear message) if knn_graph_k is too small.\ncluster_selection_method::Symbol = :eom: how to pick clusters from the condensed tree — :eom (excess of mass; the canonical HDBSCAN choice) or :leaf (return all leaves of the condensed tree, like flat DBSCAN at varying ε).\nallow_single_cluster::Bool = false: when true, the root cluster (the whole connected mass) is a candidate for EOM selection. Useful for data that is essentially one tight blob with no internal real splits — the canonical EOM rule otherwise returns zero clusters in that case. Default matches the Python hdbscan package default.\nhalo_trim_frac::Float64 = 0.10: halo-trim fraction. A point that fell out of its cluster individually (a transient / halo point) is kept only if it survived past this fraction of the cluster's λ-life; weakly-attached points that peeled off near the cluster's birth (density-connected background the MST routed onto the cluster's side) are dropped to noise. 0 disables trimming (raw condensed-tree labels — every point under a selected branch is a member); larger values trim more aggressively. Calibrate against the physical edge (the radius where cluster density crosses background); for the validated default the diffuse-cluster member radius matches that edge.\nuse_3d::Bool = false: cluster in (x, y, z); requires 3D emitters.\nper_dataset::Bool = true: cluster within each dataset index independently.\nremove_unclustered::Bool = false: drop noise emitters from the output SMLD.\n\nOutputs\n\ncluster(smld, ::HDBSCANConfig) returns (smld_out, info) matching the shared cluster interface. Per-cluster persistence (stability) and birth lambda are written to smld_out.metadata under the keys \"hdbscan_cluster_persistence\" and \"hdbscan_cluster_lambda_birth\" — flat vectors of length info.n_clusters in cluster-id order (and concatenated across datasets when per_dataset=true).\n\nExample\n\ncfg = HDBSCANConfig(min_points=10, min_cluster_size=20, knn_graph_k=50)\n(smld_out, info) = cluster(smld, cfg)\npersistence = smld_out.metadata[\"hdbscan_cluster_persistence\"]\n\nSee also: AbstractClusterConfig, ClusterInfo, cluster.\n\n\n\n\n\n","title":"SMLMClustering.HDBSCANConfig"},{"category":"type","location":"api/#SMLMClustering.HierarchicalConfig","page":"API Reference","text":"HierarchicalConfig(; cut_threshold=nothing, n_clusters=nothing, linkage=:ward,\n                    min_points=5, use_3d=false, per_dataset=true,\n                    remove_unclustered=false)\n\nConfiguration for agglomerative hierarchical clustering of SMLM localizations.\n\nExactly one of cut_threshold or n_clusters must be supplied.\n\nFields\n\ncut_threshold::Union{Float64,Nothing} = nothing: dendrogram cut height. Units depend on linkage: for distance-based linkages (:single, :complete, :average) the value is in nanometers and is converted to μm internally (h = cut_threshold / 1000.0). For :ward the dendrogram height is a variance-increase cost (roughly μm²) and is passed through without conversion — there is no meaningful \"nm\" interpretation under Ward.\nn_clusters::Union{Int,Nothing} = nothing: cut the dendrogram to produce exactly this many clusters (before min_points filtering). Natural for Ward, where cut_threshold has no intuitive unit. Mutually exclusive with cut_threshold.\nlinkage::Symbol = :ward: linkage criterion — :single, :complete, :average, or :ward. Ward minimizes within-cluster variance.\nmin_points::Int = 5: clusters with fewer than min_points members after cutting are relabeled noise (id = 0). Remaining clusters are renumbered compactly 1..K.\nuse_3d::Bool = false: include z-coordinate in distance calculation.\nper_dataset::Bool = true: cluster within each dataset independently so that (dataset, id) uniquely identifies a cluster in a multi-dataset SMLD.\nremove_unclustered::Bool = false: drop noise emitters (id == 0) from the output.\n\nnote: Scalability\nHierarchical clustering builds an O(n²) pairwise distance matrix. For large datasets (≫10,000 localizations per group) DBSCAN is preferred.\n\nExample\n\n# Distance-based: cut at 200 nm under single linkage.\ncfg = HierarchicalConfig(cut_threshold=200.0, linkage=:single)\n(smld_out, info) = cluster(smld, cfg)\n\n# Ward linkage: specify number of clusters directly.\ncfg2 = HierarchicalConfig(n_clusters=3, linkage=:ward)\n(smld_out, info) = cluster(smld, cfg2)\n\nSee also: AbstractClusterConfig, ClusterInfo, cluster.\n\n\n\n\n\n","title":"SMLMClustering.HierarchicalConfig"},{"category":"type","location":"api/#SMLMClustering.VoronoiConfig","page":"API Reference","text":"VoronoiConfig(; density_factor=2.0, min_points=5, use_3d=false,\n                per_dataset=true, remove_unclustered=false)\n\nConfiguration for Voronoi-tessellation-based (SR-Tesseler) clustering of SMLM localizations.\n\nAlgorithm\n\nBuild the Voronoi tessellation of the localization coordinates (per dataset when per_dataset=true), clipped to the convex hull so every generator has a finite cell.\nA localization is dense when its cell area is smaller than mean_cell_area / density_factor (equivalently, its local density exceeds density_factor × mean_density).\nDense points that are Delaunay-adjacent are merged into clusters via connected components.\nClusters with fewer than min_points members are relabeled noise (id = 0); the rest are renumbered compactly 1..K within the group.\n\nFields\n\ndensity_factor::Float64 = 2.0: density threshold multiplier. Higher values require stronger local density to qualify as a cluster member (smaller area threshold = fewer dense points).\nmin_points::Int = 5: minimum cluster size; smaller connected components become noise.\nuse_3d::Bool = false: must be false. 3D Voronoi clustering is not supported — DelaunayTriangulation.jl is 2D only.\nper_dataset::Bool = true: cluster within each dataset independently so that (dataset, id) uniquely identifies a cluster.\nremove_unclustered::Bool = false: drop noise emitters (id == 0) from the returned SMLD.\n\nnote: Boundary handling\nCells are clipped to the convex hull of the generator set. Generators on the hull get cells smaller than their true infinite-plane area, which can bias mean-area estimates on very small groups. In practice the effect is second-order for SMLM datasets with thousands of localizations.\n\nnote: Degenerate input\nGroups with fewer than 3 points are tagged all-noise (a tessellation requires at least 3 non-collinear points). Groups containing exact-duplicate (x,y) coordinate pairs raise ArgumentError; deduplicate input localizations before calling cluster.\n\nExample\n\ncfg = VoronoiConfig(density_factor=2.0, min_points=5)\n(smld_out, info) = cluster(smld, cfg)\n\nSee also: AbstractClusterConfig, ClusterInfo, cluster.\n\n\n\n\n\n","title":"SMLMClustering.VoronoiConfig"},{"category":"type","location":"api/#SMLMClustering.MRFDensityClusterConfig","page":"API Reference","text":"MRFDensityClusterConfig(; n_regimes=2, regime_thresholds=nothing,\n                          regime_gaussians=nothing,\n                          density_estimator=:voronoi, density_k=20,\n                          smoothness_lambda=nothing,\n                          graph_kind=:delaunay, graph_k=8,\n                          inference=:icm, icm_iters=50,\n                          min_points=5, use_3d=false,\n                          per_dataset=true, remove_unclustered=false)\n\nAdaptive-density clustering via per-emitter density → multi-component GMM regime assignment → multi-class Potts MRF refinement → connected components on the foreground.\n\nDesigned for SMLM data with multiple density regimes (e.g. one low-density background + one or more higher-density structures) where a single global ε would either over-merge or over-split. The MRF smoothness term enforces spatial coherence — points surrounded by foreground neighbors stay foreground even if their individual density is borderline (fixes \"missing middles\"); isolated tight knots in a low-density sea get pulled back to background (fixes spurious-small-cluster artifacts).\n\nDensity estimator\n\nVoronoi density (1/cell area) is the simplest local estimator but its noise scale is large (σlog ≈ 1) because it averages over a single Voronoi cell. For thin elongated structures (width comparable to nearest-neighbor distance, e.g. dSTORM fibers narrower than ~100 nm at high density) Voronoi cells span the patch boundary and inflate, dragging interior densities toward background and producing a false-negative band at patch interiors. The kNN density estimator `ρk = k / (π · rk²)` (Loftsgaarden-Quesenberry 1965) integrates over the k nearest neighbors and reduces noise to σlog ≈ 1/√k, recovering patch-interior detection at the cost of some boundary blur. Default density_estimator=:voronoi preserves backward compatibility; switch to :knn (with density_k=20-30) for thin-fiber-bearing datasets.\n\nFields\n\nn_regimes::Int = 2: number of density regimes. Lowest is treated as background/noise; higher regimes form the foreground.\nregime_thresholds::Union{Nothing, Vector{Float64}} = nothing: optional explicit log-density thresholds, length n_regimes - 1, sorted ascending. When provided, GMM is bypassed and points are binned directly with hard unaries. This is useful as a conservative low-contrast stabilizer, but borderline points cannot be rescued by spatial neighbors.\nregime_gaussians::Union{Nothing, NamedTuple} = nothing: optional calibrated Gaussian emission model (means, vars, weights) in log-density space. When provided, GMM is bypassed but soft unaries are preserved, so the Potts prior can still flip borderline interior points.\ndensity_estimator::Symbol = :voronoi: per-emitter density estimator. :voronoi uses 1/Voronoi-cell-area; :knn uses k / (π · rk²) where `rkis thedensity_k`-th nearest-neighbor distance.\ndensity_k::Int = 20: k for the kNN density estimator when density_estimator=:knn. Ignored for :voronoi. Larger k → lower variance, more boundary blur; recommended range 10-50.\nsmoothness_lambda::Union{Nothing, Float64} = nothing: MRF smoothness weight. When nothing, auto-set per group to max(1e-6, MAD(U_max - U_min)) where MAD is the median absolute deviation of the per-emitter unary range.\ngraph_kind::Symbol = :delaunay: neighbor graph for both the MRF and the CC step. :delaunay reuses the tessellation from step 1 (free, requires density_estimator=:voronoi); :knn builds a symmetrized k-NN graph (uses graph_k). When density_estimator=:knn AND graph_kind=:delaunay, a Delaunay triangulation is still computed to produce the neighbor graph.\ngraph_k::Int = 8: k for the kNN graph when graph_kind=:knn. Ignored for :delaunay.\ninference::Symbol = :icm: MRF inference algorithm. Only :icm (Iterated Conditional Modes) is supported in v1; future graph-cut inference would land here.\nicm_iters::Int = 50: maximum ICM passes; terminates early when no point changes label in a full pass.\nmin_points::Int = 5: minimum cluster size after CC; smaller components are demoted to noise.\nuse_3d::Bool = false: must be false. 3D Voronoi tessellation is not supported (DelaunayTriangulation.jl is 2D only); passing true raises ArgumentError.\nper_dataset::Bool = true: when true, run the full pipeline per dataset so each cell's density distribution is fit independently.\nremove_unclustered::Bool = false: drop noise emitters from the output.\n\nOutputs\n\nStandard (smld_out, info) tuple. Per-cluster cluster ids in emitter.id (0 = noise / lowest regime / sub-min component, 1..K = cluster). HDBSCAN-style metadata stamped onto smld_out.metadata:\n\nmetadata[\"mrf_regime_per_emitter\"]::Vector{Int} of length n_locs_in, in original emitter order. Values: 0 (ungroupable / group too small), 1 (lowest density / background), ..., n_regimes (highest density).\nmetadata[\"mrf_lambda_used\"]::Vector{Float64}: per-group λ used.\nmetadata[\"mrf_regime_means\"]::Vector{Vector{Float64}}: per-group Gaussian component means (sorted ascending) in log-density space; vector of NaNs when hard regime_thresholds were provided.\n\nExample\n\n# 2-regime auto: GMM finds the foreground/background split per dataset\ncfg = MRFDensityClusterConfig()\n(smld_out, info) = cluster(smld, cfg)\nregimes = smld_out.metadata[\"mrf_regime_per_emitter\"]\n\n# 3-regime with manual thresholds (e.g. learned from training data)\ncfg2 = MRFDensityClusterConfig(n_regimes = 3,\n                               regime_thresholds = [3.5, 5.0],\n                               min_points = 10)\n\nSee also: AbstractClusterConfig, ClusterInfo, cluster, VoronoiDensityConfig (the read-only sibling that only exposes per-emitter densities without clustering).\n\n\n\n\n\n","title":"SMLMClustering.MRFDensityClusterConfig"},{"category":"type","location":"api/#SMLMClustering.PointHysteresisConfig","page":"API Reference","text":"PointHysteresisConfig(; graph_k=12, min_points=100, use_3d=false,\n                        per_dataset=false, remove_unclustered=false)\n\nConfiguration for point-graph hysteresis seed-and-grow clustering.\n\nHolds only knobs — seed and support boolean masks are passed as required keyword arguments to cluster, not on the config. The same PointHysteresisConfig instance is reusable across many SMLDs.\n\nFields\n\ngraph_k::Int = 12: degree of the kNN graph used for the BFS.\nmin_points::Int = 100: minimum component size for a cluster to be kept. Components with <min_points members or no seed presence are dropped to noise (id = 0).\nuse_3d::Bool = false: build the kNN graph in (x, y, z); requires Emitter3DFit emitters.\nper_dataset::Bool = false: when true, cluster within each dataset independently. Cluster ids are local to each dataset's namespace so (dataset, id) is the unique identifier (V3).\nremove_unclustered::Bool = false: if true, drop emitters with id = 0 from the returned SMLD.\n\nExample\n\n# Build the per-cell feature, then thresholds, then hysteresis.\n(_, info_f) = cluster_statistics(smld, LocalContrastFeature(density_k=200,\n                                                            background_k=2000))\ncontrast = info_f.extras[:contrast_per_emitter]\nfine = info_f.extras[:log_density_per_emitter]\nfine_floor = quantile(filter(isfinite, fine), 0.35)\nseed = isfinite.(contrast) .& isfinite.(fine) .& (contrast .> 0.25) .& (fine .> fine_floor)\nsupport = isfinite.(contrast) .& isfinite.(fine) .& (contrast .> -0.05) .& (fine .> fine_floor)\n\ncfg = PointHysteresisConfig(graph_k = 12, min_points = 150)\n(smld_out, info) = cluster(smld, cfg; seed = seed, support = support)\n\nSee also: AbstractClusterConfig, LocalContrastFeature, MRFDensityClusterConfig (the GMM+Potts alternative), ClusterInfo.\n\n\n\n\n\n","title":"SMLMClustering.PointHysteresisConfig"},{"category":"function","location":"api/#SMLMClustering.calibrate_regime_gaussians","page":"API Reference","text":"calibrate_regime_gaussians(smld; n_regimes=2, density_estimator=:knn,\n                            density_k=20) -> NamedTuple\n\nFit a calibrated soft Gaussian emission model in log-density space. The returned named tuple has fields (means, vars, weights) and can be passed directly to MRFDensityClusterConfig(regime_gaussians = ...).\n\nUnlike regime_thresholds, this preserves soft unary costs: U[i, k] = -log(w_k * N(log_rho[i] | μ_k, σ_k²)). That makes the MRF behave more like a spatial hidden-state model: an emitter slightly below the foreground/background decision boundary can still become foreground when its neighbors provide enough Potts support.\n\nSee also: calibrate_regime_thresholds, MRFDensityClusterConfig.\n\n\n\n\n\n","title":"SMLMClustering.calibrate_regime_gaussians"},{"category":"function","location":"api/#SMLMClustering.calibrate_regime_thresholds","page":"API Reference","text":"calibrate_regime_thresholds(smld; n_regimes=2, density_estimator=:knn,\n                             density_k=20) -> Vector{Float64}\n\nDerive log-density regime-boundary thresholds from a calibration SMLD by fitting a 1D Gaussian mixture on its per-emitter log densities. The returned Vector{Float64} of length n_regimes - 1 is sorted ascending and is in the exact shape MRFDensityClusterConfig.regime_thresholds accepts — pass it directly into the config to bypass GMM on subsequent query ROIs.\n\nUse case\n\nFor datasets with stable density structure across ROIs (same imaging conditions, same labeling protocol), calibrating once on a trusted ROI and reusing the thresholds across query ROIs is faster (skips per-ROI EM) and more reproducible than letting the GMM auto-fit on each ROI independently. The recommendation is especially strong at the low-contrast end of the density-ratio band (Round 013 / KB V13): when the high/low log-density modes are close together, GMM EM on a single ROI can pick a degenerate split that the Potts smoothness prior then amplifies into a uniform-label collapse — calibration-derived thresholds avoid that failure mode by fixing the regime boundary at the value learned from a higher-quality fit.\n\nArguments\n\nsmld::SMLMData.BasicSMLD: the calibration ROI. The helper computes per-emitter log densities globally (no per-dataset split — the returned thresholds are a single global vector that applies uniformly across all groups when passed to MRFDensityClusterConfig.regime_thresholds).\nn_regimes::Int = 2: number of density regimes (≥ 2). Returns n_regimes - 1 thresholds.\ndensity_estimator::Symbol = :knn: density estimator for log-ρ. :knn uses density_k nearest neighbors (recommended for thin-fiber-bearing datasets per V12); :voronoi uses 1/cell-area.\ndensity_k::Int = 20: k for the kNN estimator. Ignored for :voronoi.\n\nBoundary computation\n\nEach threshold is the analytic Bayes decision boundary between consecutive GMM components (sorted ascending by mean), accounting for unequal variances and weights. Falls back to the midpoint between consecutive means when the quadratic discriminant is negative (one component dominates everywhere).\n\nReturns\n\nVector{Float64} of length n_regimes - 1, sorted ascending. Pass directly to MRFDensityClusterConfig(regime_thresholds = ...).\n\nErrors\n\nArgumentError if n_regimes < 2, density_estimator is unknown, density_k < 1, or the calibration SMLD has fewer valid emitters than n_regimes (after filtering NaN densities).\nErrorException if the GMM fit fails (variance collapse, all-equal densities). Try a different calibration ROI or fewer regimes.\n\nExample\n\n# Fit thresholds on a high-quality calibration ROI.\ncal_smld = load_calibration_data()\nthr = calibrate_regime_thresholds(cal_smld; n_regimes = 2,\n                                   density_estimator = :knn,\n                                   density_k = 20)\n\n# Apply on query ROIs without re-fitting.\ncfg = MRFDensityClusterConfig(n_regimes = 2,\n                              density_estimator = :knn,\n                              density_k = 20,\n                              regime_thresholds = thr)\n(smld_out, info) = cluster(query_smld, cfg)\n\nSee also: MRFDensityClusterConfig, cluster.\n\n\n\n\n\n","title":"SMLMClustering.calibrate_regime_thresholds"},{"category":"function","location":"api/#SMLMClustering.cluster_statistics","page":"API Reference","text":"cluster_statistics(smld::SMLMData.BasicSMLD, cfg::AbstractStatisticsConfig)\n    -> (smld, ClusterStatisticsInfo)\n\nCompute a spatial / clustering statistic on smld using the backend selected by the concrete type of cfg. Returns a tuple (smld, info).\n\nPass-through SMLD semantic (NOT non-mutating copy)\n\nUnlike cluster() — which deep-copies emitters so it can write cluster labels without touching the input — cluster_statistics() writes nothing onto the SMLD and returns the same reference as the input. The two-element tuple shape is preserved for ecosystem symmetry (every SMLM step returns (smld, info)), but callers should know the SMLD is the unmodified input, not a fresh copy. This asymmetry is intentional: copying for a read-only operation is wasted work, and statistic backends never have a label to write back.\n\nDispatch on AbstractStatisticsConfig alone has no implementation — concrete backends supply a method specialized on their own config type. Calling cluster_statistics with an unsupported config raises a clear error naming the available concrete backends.\n\nSee also: AbstractStatisticsConfig, ClusterStatisticsInfo, cluster (the labeling sibling).\n\n\n\n\n\n","title":"SMLMClustering.cluster_statistics"},{"category":"type","location":"api/#SMLMClustering.AbstractStatisticsConfig","page":"API Reference","text":"AbstractStatisticsConfig <: SMLMData.AbstractSMLMConfig\n\nAbstract supertype for cluster_statistics backend configurations.\n\nSibling to AbstractClusterConfig. Concrete subtypes (e.g. HopkinsConfig) configure spatial-statistic computations that do not modify the SMLD — they read the coordinate set, compute a scalar (and optional vector) summary, and return it alongside the input SMLD reference.\n\nShared fields (expected on every concrete subtype)\n\nuse_3d::Bool: whether the z-coordinate participates in the statistic\nper_dataset::Bool: if true, compute per dataset and aggregate\n\nAlgorithm-specific fields (n_samples, seed, random_repeats, ...) live on the concrete subtype that needs them.\n\n\n\n\n\n","title":"SMLMClustering.AbstractStatisticsConfig"},{"category":"type","location":"api/#SMLMClustering.ClusterStatisticsInfo","page":"API Reference","text":"ClusterStatisticsInfo <: SMLMData.AbstractSMLMInfo\n\nSecondary output from cluster_statistics() — summary of a spatial-statistic computation.\n\nFields\n\nn_locs_in::Int: number of input localizations\nstatistic::Float64: primary scalar result (Hopkins H, median density, ...)\nstatistic_name::Symbol: identifier for statistic (:hopkins, :median_density, ...)\nalgorithm::Symbol: backend identifier (:hopkins, :voronoi_density, ...)\nelapsed_s::Float64: wall-clock time spent in the cluster_statistics call (seconds)\nextras::Dict{Symbol,Any}: per-backend supplementary outputs — vector-valued results (e.g. per-dataset Hopkins scores under :hopkins_per_dataset, per-emitter densities under :density_per_emitter) live here\n\nConvention for vector-valued backends\n\nBackends that produce a natural vector output (per-dataset Hopkins, per-emitter density, per-cluster silhouette) place the vector in extras under a descriptive key and a meaningful summary scalar (mean, median, ...) in statistic. This keeps the simple info.statistic access ergonomic for one-number consumers while preserving the full result for callers that need it.\n\nExample\n\n(_, info) = cluster_statistics(smld, HopkinsConfig(n_samples=50, seed=1))\nprintln(\"Hopkins H = $(round(info.statistic, digits=3))\")\nper_ds = info.extras[:hopkins_per_dataset]  # Vector{Float64} when per_dataset=true\n\n\n\n\n\n","title":"SMLMClustering.ClusterStatisticsInfo"},{"category":"type","location":"api/#SMLMClustering.HopkinsConfig","page":"API Reference","text":"HopkinsConfig(; n_samples=20, random_repeats=1, seed=nothing,\n                use_3d=false, per_dataset=true)\n\nConfiguration for the Hopkins-statistic spatial-randomness test.\n\nAlgorithm\n\nFor each repeat:\n\nDraw n_samples reference points uniformly from the per-group bounding box; let u_i = NN distance from each reference point to the data.\nDraw n_samples real points without replacement from the data; let w_i = NN distance from each sampled point to the OTHER real points (excluding itself).\nWith d = 2 (or 3 if use_3d=true), H = Σuᵢ^d / (Σuᵢ^d + Σwᵢ^d).\n\nRepeats are averaged.\n\nFields\n\nn_samples::Int = 20: number of reference / sampled points per repeat. Must satisfy n_samples ≤ n_points per group.\nrandom_repeats::Int = 1: number of independent repeats to average. Higher values reduce variance at linear cost.\nseed::Union{Int,Nothing} = nothing: when set, seeds an internal Xoshiro for reproducibility. When nothing, uses the global RNG.\nuse_3d::Bool = false: include the z-coordinate (and use d=3 in the formula).\nper_dataset::Bool = true: when true, compute Hopkins per dataset; the reported statistic is the mean across datasets and the full per-dataset vector is placed in extras[:hopkins_per_dataset]. When false, all emitters are pooled and a single H is returned.\nregion = nothing: observation window for the uniform reference points (2D only). Hopkins is window-sensitive — sampling references over the data bounding box makes data that is uniform inside a non-convex boundary read as falsely clustered. Options: nothing → the data bounding box (default); a polygon Vector{NTuple{2,Float64}} → references are rejection-sampled inside it; :metadata → use the polygon at smld.metadata[\"edge_outer_polygon\"] (written by classify_emitters — the pipeline channel); Dict(dataset_id => polygon) → one polygon per dataset (per_dataset = true). Incompatible with use_3d = true.\n\nInterpretation\n\nH ≈ 0.5: data is consistent with uniform spatial randomness (Poisson)\nH → 1.0: strong clustering tendency\nH → 0.0: anti-clustering / regular spacing\n\nExample\n\ncfg = HopkinsConfig(n_samples = 50, random_repeats = 5, seed = 1)\n(_, info) = cluster_statistics(smld, cfg)\nprintln(\"Hopkins H = $(round(info.statistic, digits=3))\")\n\nSee also: AbstractStatisticsConfig, ClusterStatisticsInfo, cluster_statistics.\n\n\n\n\n\n","title":"SMLMClustering.HopkinsConfig"},{"category":"type","location":"api/#SMLMClustering.VoronoiDensityConfig","page":"API Reference","text":"VoronoiDensityConfig(; use_3d=false, per_dataset=true)\n\nConfiguration for the per-emitter Voronoi-density spatial-statistic backend.\n\nAlgorithm\n\nFor each group (dataset when per_dataset=true, all emitters otherwise), build the Voronoi tessellation of the emitter coordinates clipped to the convex hull (so every generator has a finite cell area).\nFor each emitter i, compute Aᵢ = its Voronoi cell area (μm²) and ρᵢ = 1/Aᵢ (μm⁻²).\nStitch the per-group vectors back into original emitter order so extras[:density_per_emitter][i] corresponds to smld.emitters[i].\n\nFields\n\nuse_3d::Bool = false: must be false. 3D Voronoi tessellation is not supported (DelaunayTriangulation.jl is 2D only); passing true raises ArgumentError.\nper_dataset::Bool = true: when true, tessellate each dataset independently. When false, all emitters are pooled into one tessellation. Per-emitter outputs remain flat in original emitter order in either case.\n\nReturned info\n\nstatistic = median(non-NaN densities) — single number summarizing overall density. NaN if no group produced any valid density.\nstatistic_name = :median_density\nalgorithm = :voronoi_density\nextras[:density_per_emitter] — Vector{Float64} of length n_locs_in, in original emitter order. Emitters in groups smaller than 3 (untessellatable) receive NaN. Units: μm⁻².\nextras[:area_per_emitter] — Vector{Float64} of length n_locs_in, in original emitter order. Same NaN semantics. Units: μm².\n\nEdge case handling (per V10 NaN-vs-throw rule)\n\nGroups with fewer than 3 points: those emitters receive NaN density and NaN area. Other groups proceed normally.\nEmpty SMLD: empty per-emitter vectors, statistic = NaN.\nGroup with exact-duplicate (x, y) coordinates: ArgumentError raised before triangulation (mirrors VoronoiConfig's guard — duplicate coordinates are a boundary-input issue, not a data-shape edge case).\n\nExample\n\ncfg = VoronoiDensityConfig()\n(_, info) = cluster_statistics(smld, cfg)\nρ = info.extras[:density_per_emitter]   # Vector{Float64}, length == n_locs_in\nA = info.extras[:area_per_emitter]\nprintln(\"median density = $(round(info.statistic, digits=2)) μm⁻²\")\n\nSee also: AbstractStatisticsConfig, ClusterStatisticsInfo, cluster_statistics, VoronoiConfig (the clustering sibling).\n\n\n\n\n\n","title":"SMLMClustering.VoronoiDensityConfig"},{"category":"type","location":"api/#SMLMClustering.LocalContrastFeature","page":"API Reference","text":"LocalContrastFeature(; density_k=200, background_k=2000,\n                      use_3d=false, per_dataset=false)\n\nPer-emitter local-density-contrast feature.\n\nAlgorithm\n\nBuild a KDTree over the (per-dataset or pooled) emitter coordinates.\nFor each emitter i, compute the kNN log-density f_i = log(density_k / (π · r_k²)) where r_k is the distance to the density_k-th nearest neighbor (excluding self). The kNN ball area normalization gives a density estimate in coordinate-units⁻²; the log keeps the downstream median/threshold comparisons in a comparable scale across a cell.\nFor each emitter i, compute the median of f_j over its background_k nearest neighbors (excluding self) — the local baseline at a coarser spatial scale. Median is used (not mean) because it tolerates a small number of locally-elevated neighbors without dragging the baseline up.\nContrast c_i = f_i − median(f_j over j in k_bg-NN of i). Positive contrast means point i is denser than its local surroundings.\n\nFields\n\ndensity_k::Int = 200: fine-scale neighborhood for the per-point log-density. Sets the spatial scale of the signal; for SMLM cells, ~80–200 nm typically.\nbackground_k::Int = 2000: coarse-scale neighborhood for the local baseline (must be > density_k). Sets the spatial scale of the baseline; should be large enough to span the structures you want to detect (typically 1–2 μm).\nuse_3d::Bool = false: if true, build the KDTree over (x, y, z).\nper_dataset::Bool = false: if true, compute per dataset independently (each dataset gets its own KDTree). Per-emitter outputs stitch back into original emitter order in either case.\n\nReturned info\n\nstatistic = median(non-NaN contrast) — single scalar summary. NaN when no group has enough points to compute the feature.\nstatistic_name = :median_local_contrast\nalgorithm = :local_contrast\nextras[:contrast_per_emitter] — Vector{Float64} of length n_locs_in, in original emitter order. Units: nat (natural-log scale). The caller thresholds this directly.\nextras[:log_density_per_emitter] — Vector{Float64} of length n_locs_in, the fine kNN log-density f_i. Useful for absolute-density gates that complement the local-contrast gate (e.g. require f_i > q35 floor).\n\nEdge case handling\n\nGroups with n ≤ density_k: those emitters receive NaN for both contrast and log-density. Avoids a degenerate kNN query against a too-small set.\nbackground_k ≥ n in a group: clamped to n − 1 for that group; the feature becomes \"log-density minus group median,\" which is still well-defined.\nbackground_k ≤ density_k: raises ArgumentError at config use; the baseline must be coarser than the signal.\nCoincident coordinates (r_k = 0): the affected emitter receives NaN log-density and NaN contrast.\n\nComposition\n\nThis feature is the missing primitive for hysteresis seed-and-grow on point clouds with non-stationary baseline density:\n\n(_, info) = cluster_statistics(smld, LocalContrastFeature())\ncontrast = info.extras[:contrast_per_emitter]\nfine = info.extras[:log_density_per_emitter]\nfine_floor = quantile(filter(isfinite, fine), 0.35)\nseed = isfinite.(contrast) .& (contrast .> 0.25) .& (fine .> fine_floor)\nsupport = isfinite.(contrast) .& (contrast .> -0.05) .& (fine .> fine_floor)\n(smld_out, _) = cluster(smld, PointHysteresisConfig(graph_k=12, min_points=150);\n                        seed=seed, support=support)\n\nSee also: AbstractStatisticsConfig, ClusterStatisticsInfo.\n\n\n\n\n\n","title":"SMLMClustering.LocalContrastFeature"},{"category":"function","location":"api/#SMLMClustering.EdgeClassify.classify_emitters","page":"API Reference","text":"classify_emitters(smld::BasicSMLD, cfg::AbstractEdgeClassifyConfig) -> (smld, info)\nclassify_emitters(x_um, y_um, cfg::AbstractEdgeClassifyConfig; fov_um) -> info\n\nClassify each emitter as :outside, :membrane, or :interior. The concrete config type selects the tissue gate by dispatch (OuterPolygonConfig, KdeValleyConfig); the rest of the pipeline is shared.\n\nThe SMLD method follows the package (out, Info) convention: it returns the smld with the published multi-cell mask threaded into smld.metadata[\"edge_cells\"] (and the dominant cell's outer ring into smld.metadata[\"edge_outer_polygon\"] for back-compat / Hopkins region=:metadata), plus an EdgeClassifyInfo. The per-emitter class lives only in info.class (with the in_cell / interior_mask accessors) — it is deliberately not mirrored into the metadata, because a per-emitter side-list desyncs the moment a downstream step subsets emitters. The coordinate method is the computational core and returns the info directly; fov_um = (xmin, xmax, ymin, ymax) in µm.\n\n\n\n\n\n","title":"SMLMClustering.EdgeClassify.classify_emitters"},{"category":"type","location":"api/#SMLMClustering.EdgeClassify.AbstractEdgeClassifyConfig","page":"API Reference","text":"Config types for classify_emitters.\n\nAbstractEdgeClassifyConfig is a sibling of the package's AbstractClusterConfig / AbstractStatisticsConfig (all <: SMLMData.AbstractSMLMConfig); each concrete config is dispatched as a method of classify_emitters:\n\nOuterPolygonConfig — multi-K density gate → multi-cell alpha-shape mask → point-in-region + membrane band.\nKdeValleyConfig — adaptive dSTORM density-valley gate (Gaussian-KDE + valley + footprint); gates on the original cloud, then builds the multi-cell mask on the footprint subset.\n\nStruct fields are lowercase (idiomatic Julia); the UPPERCASE params.json keys are produced only at the serialization boundary by to_dict.\n\n\n\n\n\n","title":"SMLMClustering.EdgeClassify.AbstractEdgeClassifyConfig"},{"category":"type","location":"api/#SMLMClustering.EdgeClassify.OuterPolygonConfig","page":"API Reference","text":"OuterPolygonConfig(; alpha_nm=300, membrane_nm=100,\n                   fov_trunc_tol_nm=150, k_list=(16,128), rho_k_thresh=200)\n\nPoint-in-region vs the multi-cell alpha-shape mask on the multi-K-density-gated set, plus a membrane_nm band.\n\n\n\n\n\n","title":"SMLMClustering.EdgeClassify.OuterPolygonConfig"},{"category":"type","location":"api/#SMLMClustering.EdgeClassify.KdeValleyConfig","page":"API Reference","text":"KdeValleyConfig(; alpha_nm=600, membrane_nm=100,\n                fov_trunc_tol_nm=150, sigma_nm=150, ...)\n\nAdaptive density-valley gate for dSTORM data. Gates on the per-FOV KDE density valley (threshold-free, no per-cell tuning). The defaults are tuned for dSTORM membrane data — notably alpha_nm = 600 (vs. the polygon default of 300) — so a bare KdeValleyConfig() is the intended entry point.\n\n\n\n\n\n","title":"SMLMClustering.EdgeClassify.KdeValleyConfig"},{"category":"type","location":"api/#SMLMClustering.EdgeClassify.EdgeClassifyInfo","page":"API Reference","text":"EdgeClassifyInfo{C} <: SMLMData.AbstractSMLMInfo\n\nResult of classify_emitters. class is the authoritative per-emitter answer (:outside, :membrane, :interior), a partition of the input set.\n\ncells::MultiCellMask is the published mask: one CellPolygon (an outer ring plus optional internal holes) per distinct cell in the FOV, ordered largest-first. It is the drawn boundary and the Hopkins region = :metadata observation window. outer_polygon is cells[1].outer (the dominant cell's outer ring), retained for back-compat.\n\ninside_outer is geometric membership in the mask (in_region(cells)), and dist_to_outer_um is the distance to the nearest real boundary segment (NaN when not inside). Boundary segments lying on a truncated FOV edge are excluded from that distance, so a field-of-view cut is never labeled membrane — membrane is the band within membrane_nm of a real cell edge. in_cell(info) (class .!= :outside) equals inside_outer. Read class for the answer.\n\nloops are the raw alpha-shape boundary loops (serialized to polygon_loops.tsv); config holds the concrete config that ran (honest provenance).\n\n\n\n\n\n","title":"SMLMClustering.EdgeClassify.EdgeClassifyInfo"},{"category":"function","location":"api/#SMLMClustering.EdgeClassify.in_cell","page":"API Reference","text":"in_cell(info) -> BitVector\n\nTopological cell membership, == (info.class .!= :outside). Equals inside_outer (both are mask membership via in_region).\n\n\n\n\n\n","title":"SMLMClustering.EdgeClassify.in_cell"},{"category":"function","location":"api/#SMLMClustering.EdgeClassify.interior_mask","page":"API Reference","text":"interior_mask(info) -> BitVector\n\nPer-emitter interior mask, == (info.class .== :interior) — the strictly-interior emitters (membrane and outside both excluded). Unlike in_cell (which keeps membrane), this is the subset a downstream analysis usually carries forward, and is the boolean to AND with any other per-emitter mask (e.g. a separate structure mask) before subsetting. classify_emitters stays non-destructive — it never drops emitters — so the caller composes masks and subsets once.\n\n\n\n\n\n","title":"SMLMClustering.EdgeClassify.interior_mask"},{"category":"function","location":"api/#SMLMClustering.EdgeClassify.interior_fraction","page":"API Reference","text":"interior_fraction(info) -> Float64\n\n\n\n\n\n","title":"SMLMClustering.EdgeClassify.interior_fraction"},{"category":"type","location":"api/#SMLMClustering.CellPolygon","page":"API Reference","text":"CellPolygon(outer, holes = [])\n\nOne cell's footprint: an outer boundary ring (a Vector of (x, y) µm tuples, stored open / not repeating the first vertex) plus optional internal holes (each a ring). holes is empty unless the mask was built with keep_internal = true.\n\n\n\n\n\n","title":"SMLMClustering.CellPolygon"},{"category":"type","location":"api/#SMLMClustering.MultiCellMask","page":"API Reference","text":"MultiCellMask\n\nVector{CellPolygon} — the edge mask of a field of view: one CellPolygon per distinct cell. Returned by classify_emitters (info.cells / metadata[\"edge_cells\"]) and accepted as a Hopkins observation window. Cells are ordered largest-first, so mask[1] is the dominant cell.\n\n\n\n\n\n","title":"SMLMClustering.MultiCellMask"},{"category":"function","location":"api/#SMLMClustering.build_mask","page":"API Reference","text":"build_mask(loops; keep_internal = false, min_cell_frac = 1/3) -> MultiCellMask\n\nAssemble a MultiCellMask from raw alpha-shape boundary loops (closed vertex rings):\n\nSplit each loop into simple sub-rings at self-touch / shared vertices.\nNest by depth (how many other rings contain it): even depth ⇒ a cell outer, odd depth ⇒ an internal region (a real void or a pinch-enclosed region).\nGroup each cell outer with the internal regions directly inside it — only when keep_internal = true; otherwise cells are solid (holes empty).\nDrop debris: remove any cell whose outer area is below min_cell_frac × (largest cell area); min_cell_frac = 0 keeps everything.\n\nCells are returned largest-first.\n\n\n\n\n\n","title":"SMLMClustering.build_mask"},{"category":"function","location":"api/#SMLMClustering.in_region","page":"API Reference","text":"in_region(x, y, mask::MultiCellMask) -> Bool\n\nTrue when (x, y) lies in any cell of mask: inside a cell's outer ring and not inside one of that cell's holes.\n\n\n\n\n\n","title":"SMLMClustering.in_region"},{"category":"function","location":"api/#SMLMClustering.region_area","page":"API Reference","text":"region_area(mask::MultiCellMask) -> Float64\n\nTotal occupied area in µm²: Σ cell outer areas − Σ hole areas.\n\n\n\n\n\n","title":"SMLMClustering.region_area"},{"category":"type","location":"api/#SMLMClustering.EdgeReport","page":"API Reference","text":"EdgeReport\n\nFigure-data derivative of an EdgeClassifyInfo: the classified SMLD plus per-emitter coordinates/classes and per-class fractions needed to write diagnostics and render the standard edge figures. Produced by compute_edge_report; consumed by write_edge_report and plot_edge_report.\n\n\n\n\n\n","title":"SMLMClustering.EdgeReport"},{"category":"function","location":"api/#SMLMClustering.compute_edge_report","page":"API Reference","text":"compute_edge_report(smld, info::EdgeClassifyInfo) -> EdgeReport\n\nBundle the classified SMLD and info into the figure-data report. Pass the SMLD returned by classify_emitters — its emitters are unchanged, so info.class is 1:1 with smld.emitters.\n\n\n\n\n\n","title":"SMLMClustering.compute_edge_report"},{"category":"function","location":"api/#SMLMClustering.write_edge_report","page":"API Reference","text":"write_edge_report(report; output_dir, condition=\"\", cell=\"\") -> String\n\nWrite the edge-classify text/TSV diagnostics (classified.tsv, polygonloops.tsv, loopdiagnostics.csv, params.json, manifest.json) for report into output_dir (folds write_edge_artifacts). Returns output_dir.\n\n\n\n\n\n","title":"SMLMClustering.write_edge_report"},{"category":"function","location":"api/#SMLMClustering.class_codes","page":"API Reference","text":"class_codes(info::EdgeClassifyInfo) -> Vector{Int}\n\nPer-emitter integer class code for categorical rendering: outside = 0, membrane = 1, interior = 2. The 0 for outside matches SMLMRender's reserved-gray id.\n\n\n\n\n\n","title":"SMLMClustering.class_codes"},{"category":"function","location":"api/#SMLMClustering.EdgeClassify.method_name","page":"API Reference","text":"method_name(cfg) -> String\n\nShort identifier for the edge-classification strategy selected by cfg's concrete type: \"outer_polygon\" for OuterPolygonConfig and \"kde_valley\" for KdeValleyConfig. Used in diagnostics, logging, and output manifests.\n\n\n\n\n\n","title":"SMLMClustering.EdgeClassify.method_name"},{"category":"function","location":"api/#SMLMClustering.EdgeClassify.write_edge_artifacts","page":"API Reference","text":"write_edge_artifacts(leaf, info::EdgeClassifyInfo, x_um, y_um; condition, cell,\n                     smld_input_meta=nothing)\n\nWrite the diagnostic artifact set (classified.tsv, polygon_loops.tsv, loop_diagnostics.csv, params.json, manifest.json) into leaf. x_um/y_um are the original coordinates (the info does not retain them). Compute and IO are separate: classify_emitters does not write artifacts.\n\n\n\n\n\n","title":"SMLMClustering.EdgeClassify.write_edge_artifacts"},{"category":"function","location":"api/#SMLMClustering.EdgeClassify.compute_concavity_metric","page":"API Reference","text":"compute_concavity_metric(info::EdgeClassifyInfo, x_um, y_um;\n                         buffer_um = 2.0, asym_R_nm = 1000.0, asym_gate = 0.20,\n                         rho_lo = 200.0, intracellular_dense_threshold = 0.8)\n    -> ConcavityMetricReport\n\nBoundary-proximal concavity-error metric for the outer-polygon classifier. Identifies emitters classified :interior that live in deep concave bays the alpha-shape bridged across. Measured against the dominant cell (cells[1].outer); on a multi-cell FOV it characterizes that cell only.\n\nA :interior emitter is a suspect iff: (1) it lies within buffer_um of the outer polygon boundary; (2) it is not inside an \"intracellular void\" loop (non-outer loop with frac_in_fov == 1 and frac_dense >= intracellular_dense_threshold); (3) its directional asymmetry at asym_R_nm is >= asym_gate; (4) its local density ρK(K=128) is `<= rholo`.\n\nbuffer_um is a diagnostic parameter (default 2.0 µm), not part of the classifier config.\n\n\n\n\n\n","title":"SMLMClustering.EdgeClassify.compute_concavity_metric"},{"category":"type","location":"api/#SMLMClustering.EdgeClassify.ConcavityMetricReport","page":"API Reference","text":"ConcavityMetricReport\n\nBoundary-proximal concavity-error metric for the outer-polygon classifier (see compute_concavity_metric).\n\n\n\n\n\n","title":"SMLMClustering.EdgeClassify.ConcavityMetricReport"},{"category":"type","location":"api/#SMLMClustering.EdgeClassify.LoopDiagnostic","page":"API Reference","text":"LoopDiagnostic\n\nPer-loop diagnostic record (one row of loop_diagnostics.csv).\n\n\n\n\n\n","title":"SMLMClustering.EdgeClassify.LoopDiagnostic"},{"category":"section","location":"methods/edge_classify/#Edge-/-Membrane-Classification","page":"Edge classification","text":"Classify each 2D SMLM emitter as :outside, :membrane, or :interior — the off-cell background, the cell-boundary band, and the cell interior. The verb classify_emitters is a peer of the package's cluster / cluster_statistics: the concrete config type selects the tissue-gate strategy by dispatch, and the result is an EdgeClassifyInfo. A field of view may hold more than one cell — the published boundary is a multi-cell mask.\n\n(Image: Edge classification of a synthetic cell)\n\nA synthetic cell classified into interior (blue), membrane (orange), and outside background (gray).","title":"Edge / Membrane Classification"},{"category":"section","location":"methods/edge_classify/#API","page":"Edge classification","text":"smld_out, info = classify_emitters(smld::BasicSMLD, cfg::AbstractEdgeClassifyConfig)\ninfo           = classify_emitters(x_um, y_um, cfg::AbstractEdgeClassifyConfig; fov_um)\n\nSMLD form (pipeline-facing, (out, Info) convention): returns the smld with the published mask threaded into smld.metadata[\"edge_cells\"] (a MultiCellMask) and the dominant cell's outer ring into smld.metadata[\"edge_outer_polygon\"] (back-compat / Hopkins region = :metadata), plus the EdgeClassifyInfo. fov_um is taken from smld.camera.pixel_edges_x/y. The per-emitter class is not mirrored into metadata — it lives in info.class, read via in_cell(info) / interior_mask(info). (A per-emitter side-list in metadata would silently desync the moment a downstream step subsets emitters; only the geometry — safe under subsetting — is mirrored.)\nCoordinate form (computational core): returns the info directly. fov_um = (xmin, xmax, ymin, ymax) in µm (accepts any Real tuple).","title":"API"},{"category":"section","location":"methods/edge_classify/#Concept","page":"Edge classification","text":"The pipeline is shared across configs; only the first step (the tissue gate) is config-specific:\n\nTissue gate — decide which localizations are cell tissue (per-config, below).\nRelative-density gate — drop isolated outlier whiskers whose local count is below core_frac × the tissue median (relative, so it self-scales with density; 0 disables it).\nMulti-scale adaptive alpha-shape — build boundary loops with a per-triangle α (below).\nMulti-cell mask — group the loops into cells with build_mask.\nPer-emitter labeling — :interior inside a cell, :membrane within membrane_nm of a real cell edge (FOV-cut boundary segments are excluded, so a field-of-view cut is never mislabeled membrane), :outside otherwise.","title":"Concept"},{"category":"section","location":"methods/edge_classify/#Multi-scale-adaptive-α","page":"Edge classification","text":"With alpha_adaptive = true (default), each Delaunay triangle T is kept iff its circumradius is at most\n\nalpha(T) = minBig( s cdot tilde d_textlocal(T) max(alpha_textnm s cdot tilde d_textcell) Big)\n\nwhere s = alpha_scale, tilde d_textlocal(T) is the median alpha_knn-th nearest-neighbor spacing of T's three vertices, tilde d_textcell is that spacing's per-cell median, and alpha_textnm = alpha_nm. The local term (first argument) scales to the local point spacing (∝ 1/√ρ): α shrinks where the cloud is dense — carving real concavities — and grows where it is sparse — bridging inter-clump gaps. The conservative envelope (second argument) caps how far α can grow, rejecting far-reaching low-density-noise protrusions, while its alpha_nm floor stays loose enough to hold a genuinely diffuse cell background together. Because it is a min, the cap can only remove triangles from the local shape, never add bridges. Setting alpha_adaptive = false uses a single fixed α = alpha_nm.","title":"Multi-scale adaptive α"},{"category":"section","location":"methods/edge_classify/#Multi-cell-mask","page":"Edge classification","text":"build_mask turns the kept loops into a MultiCellMask (a Vector{CellPolygon}): it splits self-touching loops into simple rings, groups outer rings with any enclosed holes by nesting parity, drops debris smaller than min_cell_frac × the largest cell's area, and orders the cells largest-first (so cells[1] is the dominant cell). Each CellPolygon has an outer ring plus optional internal holes; keep_internal (default false) fills internal voids into solid cells, true carves them out. in_region(x, y, mask) tests membership and region_area(mask) sums the enclosed area. The same MultiCellMask is accepted directly as a Hopkins observation window.","title":"Multi-cell mask"},{"category":"section","location":"methods/edge_classify/#Strategies-(configs)","page":"Edge classification","text":"Each config is a <: AbstractEdgeClassifyConfig (sibling of AbstractClusterConfig) holding only its own parameters; fields are lowercase, validated at dispatch entry. Both share the mask-pipeline parameters (core_frac, core_radius_nm, alpha_adaptive, alpha_knn, alpha_scale, keep_internal, min_cell_frac) and differ only in the gate.","title":"Strategies (configs)"},{"category":"section","location":"methods/edge_classify/#OuterPolygonConfig","page":"Edge classification","text":"Fixed multi-K k-NN density gate → multi-scale alpha-shape mask → point-in-region + membrane_nm band.\n\nfield default unit meaning\nalpha_nm 300 nm envelope floor / fixed α when alpha_adaptive=false\nmembrane_nm 100 nm membrane band width inboard of a cell edge\nfov_trunc_tol_nm 150 nm FOV-truncation detection tolerance\nk_list (16, 128) — k-NN K values for the multi-K density gate (intersection)\nrho_k_thresh 200 µm⁻² per-K density gate threshold\ncore_frac 0.10 — relative-density gate: drop < frac × median (0 disables)\ncore_radius_nm 600 nm relative-gate neighborhood radius\nalpha_adaptive true — multi-scale α (local carver ∩ per-cell envelope)\nalpha_knn 5 — k for the adaptive-α length scale\nalpha_scale 2.0 — ×local k-NN (carver) and ×cell-median (envelope)\nkeep_internal false — keep internal holes (else solid cells)\nmin_cell_frac 1/3 — drop cells < frac × largest (0 keeps all)","title":"OuterPolygonConfig"},{"category":"section","location":"methods/edge_classify/#KdeValleyConfig","page":"Edge classification","text":"Adaptive density-valley gate for dSTORM data. Gaussian-KDE density on the original cloud → background/cell valley threshold → footprint fill → the shared multi-scale mask on the footprint subset. The valley threshold is found per FOV, so there is no per-cell density tuning — which is what lets it generalize across cells whose absolute density varies. The defaults (notably alpha_nm = 600, vs. the polygon default of 300) are tuned for dSTORM membrane data, so a bare KdeValleyConfig() is the intended entry point.\n\nfield default unit meaning\nalpha_nm 600 nm envelope floor (dSTORM-tuned)\nmembrane_nm 100 nm membrane band width\nfov_trunc_tol_nm 150 nm FOV-truncation tolerance\nsigma_nm 150 nm Gaussian-KDE bandwidth σ\nrmax_sigma 3.0 — KDE range-query cutoff in units of σ\nvalley_nbins 140 — log-density histogram bins for the valley threshold\nvalley_floorfrac 0.05 — left-base cutoff as a fraction of the cell-mode peak\nvalley_smooth 4 bins ± window for histogram smoothing\nfootprint_bin_um 0.2 µm raster bin for the footprint fill\nfootprint_closing_px 3 px morphological closing radius (seal thin necks)\n\n(plus the shared mask-pipeline fields listed under OuterPolygonConfig.)","title":"KdeValleyConfig"},{"category":"section","location":"methods/edge_classify/#Choosing-a-strategy","page":"Edge classification","text":"The two strategies split along gate vs. geometry. Both end in the same multi-scale alpha-shape multi-cell mask — they differ only in how they decide which localizations are cell tissue:\n\nOuterPolygonConfig gates on a fixed multi-K kNN density threshold: fast and simple, but the threshold is absolute, so it must be set for your dataset's density and works best when density is homogeneous across FOVs.\nKdeValleyConfig gates on the valley of the per-FOV KDE density histogram — an adaptive, threshold-free criterion that handles heterogeneous cell-to-cell density without per-cell tuning.\n\nPrefer KdeValleyConfig (the recommended default) unless your FOVs are uniform in density and you want the simpler, faster fixed-threshold gate.","title":"Choosing a strategy"},{"category":"section","location":"methods/edge_classify/#Result-—-EdgeClassifyInfo","page":"Edge classification","text":"EdgeClassifyInfo{C} <: SMLMData.AbstractSMLMInfo. Key fields:\n\nfield type meaning\nclass Vector{Symbol} authoritative per-emitter answer: :outside / :membrane / :interior\ncells MultiCellMask the published mask — one CellPolygon per cell, largest-first\nouter_polygon polygon cells[1].outer (dominant cell), retained for convenience / Hopkins\ninside_outer BitVector geometric mask membership (in_region(cells)); equals class .!= :outside\ndist_to_outer_um Vector{Float64} distance to the nearest real (non-FOV-cut) boundary segment\nloops polygons the raw alpha-shape boundary loops (serialized diagnostic)\nloop_diagnostics Vector{LoopDiagnostic} per-loop diagnostics\nconfig C the concrete config that ran (provenance)\nfov_um, truncated_sides, runtime_s — run metadata\nn_outside, n_membrane, n_interior Int class counts\n\nAccessors: in_cell(info) = class .!= :outside (interior or membrane); interior_mask(info) = class .== :interior (strictly interior — the usual downstream subset, and the boolean to AND with a separate per-emitter mask before subsetting); interior_fraction(info).\n\nClass semantics. class is the canonical answer — filter on class, never on inside_outer. :outside ∪ :membrane ∪ :interior partitions the input set (no nulls, no overlaps) and the order matches the input. classify_emitters is non-destructive: it never drops emitters, so info.class is 1:1 with the input and a downstream step that subsets emitters must subset the class alongside (which is why it lives in the info, not in the auto-riding metadata).","title":"Result — EdgeClassifyInfo"},{"category":"section","location":"methods/edge_classify/#Figures-(extension)","page":"Edge classification","text":"Standard report + figures live in SMLMClusteringFiguresExt, a weak-dependency extension that loads when both CairoMakie and SMLMRender are present (mirroring SMLMBaGoL's Makie/Render extensions), so the core package carries no plotting dependencies:\n\nusing SMLMClustering, CairoMakie, SMLMRender     # loading both activates the extension\nsmld_out, info = classify_emitters(smld, KdeValleyConfig())\nreport = compute_edge_report(smld_out, info)     # core: a figure-data derivative\nwrite_edge_report(report; output_dir = dir)      # core: text/TSV diagnostics\nplot_edge_report(report; output_dir = dir)       # extension: the figure series\n\ncompute_edge_report(smld, info) (core) bundles the classified SMLD + info into an EdgeReport; write_edge_report(report; output_dir) (core) writes the text diagnostics (folds write_edge_artifacts).\nplot_edge_report(report; output_dir, zoom_render = 20, prefix = \"edge\") (extension) writes the figure series and returns the saved paths: <prefix>_render.png (an SMLMRender Gaussian super-resolution render at zoom_render — ≈ 5 nm/px for a ~100 nm camera — colored by class, the class image), <prefix>_overlay.png (a CairoMakie polygon overlay over class-colored localizations) and <prefix>_fractions.png (a class-fraction bar).\nrender_classes(smld, class; ...) is the underlying per-class renderer; class_codes(info) maps the class to the integer code the render uses (outside = 0 → SMLMRender's reserved gray, membrane = 1, interior = 2).","title":"Figures (extension)"},{"category":"section","location":"methods/edge_classify/#Artifacts-(write_edge_artifacts)","page":"Edge classification","text":"Written under <out_dir>/<condition>/<cell>/. Schemas are stamped in headers / manifest.json; per-config params are serialized via the to_dict trait (only the fields that actually ran are recorded).\n\nfile schema contents\nclassified.tsv 2 emitter_id, x_um, y_um, class, inside_outer, in_cell, dist_to_outer_um\npolygon_loops.tsv 2 all alpha-shape loops (loop_id, vertex_id, x_um, y_um)\nloop_diagnostics.csv 2 per-loop diagnostics\nparams.json 3 git provenance, fov, truncation, params (method-specific via to_dict), runtime\nmanifest.json 1 artifact index + schema versions\n\nparams.json carries params[\"METHOD\"] = method_name(cfg) (\"outer_polygon\" / \"kde_valley\") as a write-only provenance label.","title":"Artifacts (write_edge_artifacts)"},{"category":"section","location":"methods/edge_classify/#Concavity-metric-(diagnostic)","page":"Edge classification","text":"report = compute_concavity_metric(info, x_um, y_um; buffer_um=2.0, ...)\n\nFlags :interior emitters of the dominant cell (cells[1]) that sit in deep concave bays the alpha-shape bridged across (boundary-proximal, high directional asymmetry, low local density), stratified by whether the nearest outer segment is inside the FOV or straddles its edge. Diagnostic only — it does not change class. Returns a ConcavityMetricReport.","title":"Concavity metric (diagnostic)"},{"category":"section","location":"methods/edge_classify/#References","page":"Edge classification","text":"Alpha shapes (the boundary primitive): Edelsbrunner & Mücke, \"Three-dimensional alpha shapes\", ACM Trans. Graph. 13(1), 43–72 (1994), doi:10.1145/174462.156635. Concave-hull / χ-shape variants are reasonable alternatives for the boundary loop.\nRelated density-segmentation lineage (the contrast for the density gate): Levet et al., \"SR-Tesseler\", Nat. Methods 12, 1065–1071 (2015), doi:10.1038/nmeth.3579 — Voronoi-area density thresholded at a valley; KdeValleyConfig is the KDE analogue of that idea.","title":"References"},{"category":"section","location":"methods/hierarchical/#Hierarchical","page":"Hierarchical","text":"Agglomerative hierarchical clustering of SMLM localizations, exposed as a cluster labeling backend through HierarchicalConfig. It is backed by Clustering.hclust + cutree: a per-group pairwise distance matrix is built, merged bottom-up into a dendrogram, then cut into flat clusters either at a height (cut_threshold) or to a fixed number of clusters (n_clusters).\n\n(Image: Single-linkage chaining vs Ward)\n\nCut to two clusters on two blobs joined by a thin bridge. Single linkage chains across the bridge, so both blobs land in the same cluster (the 2-cluster cut only peels off a stray point); Ward keeps the blobs apart (right).","title":"Hierarchical"},{"category":"section","location":"methods/hierarchical/#Concept","page":"Hierarchical","text":"Agglomerative clustering starts with every localization in its own singleton cluster and repeatedly merges the two closest clusters until a single tree (the dendrogram) remains. \"Closest\" is defined by a linkage criterion that extends the point-to-point distance to a cluster-to-cluster distance. The backend supports four linkages:\n\n:single — nearest-neighbor distance between the two clusters (chaining-prone, finds elongated structure).\n:complete — farthest-neighbor distance (compact, roughly spherical clusters).\n:average — mean pairwise distance between the clusters (UPGMA, a middle ground).\n:ward — merge the pair whose union increases total within-cluster variance the least; tends to produce balanced, compact clusters.\n\nThe dendrogram records, at every merge, the height (linkage distance) at which it happened. A flat clustering is obtained by cutting the tree: either horizontally at a chosen height — every subtree joined below that height becomes a cluster — or by requesting exactly a target number of clusters. Cutting by height answers \"group everything closer than h\"; cutting by count answers \"give me K groups\", and is the natural choice when the height has no intuitive unit.","title":"Concept"},{"category":"section","location":"methods/hierarchical/#How-it-works","page":"Hierarchical","text":"For each group of localizations (one group per dataset when per_dataset = true, otherwise the whole SMLD), the backend:\n\nExtracts a d times n coordinate matrix in microns (d = 2, or d = 3 when use_3d = true).\nBuilds the symmetric n times n Euclidean pairwise-distance matrix\nD_ij = lVert x_i - x_j rVert_2 \nso D is in microns. This O(n^2) matrix is the dominant cost.\nRuns Clustering.hclust(D, linkage = ...) to build the dendrogram, then Clustering.cutree to flatten it.\nRelabels any cluster with fewer than min_points members as noise (id = 0); surviving clusters are renumbered compactly 1K within the group.","title":"How it works"},{"category":"section","location":"methods/hierarchical/#Linkage-math","page":"Hierarchical","text":"At each step the two clusters A and B minimizing the cluster distance d(A B) are merged. The four linkages differ only in d(A B):\n\nbeginaligned\ntextsingle   quad d(AB) = min_a in A b in B D_ab \ntextcomplete quad d(AB) = max_a in A b in B D_ab \ntextaverage  quad d(AB) = frac1AB sum_a in A sum_b in B D_ab \ntextward     quad d(AB) = sqrtfrac2ABA + B lVert mu_A - mu_B rVert_2 \nendaligned\n\nwhere mu_A mu_B are the cluster centroids. For single/complete/average the merge height is a genuine distance (microns, inherited from D). For Ward the height tracks the increase in total within-cluster variance caused by the merge — a cost with units of roughly micron² rather than a distance.","title":"Linkage math"},{"category":"section","location":"methods/hierarchical/#The-cut-and-its-unit-convention","page":"Hierarchical","text":"Exactly one of cut_threshold or n_clusters controls the cut:\n\ncut_threshold (cut by height). For the distance-based linkages (:single, :complete, :average) the value is given in nanometers and converted to microns internally before cutting, to match the micron-valued dendrogram height:\nh = fractextttcut_threshold1000 quad (textnm rightarrow mutextm)\nFor :ward the dendrogram height returned by Clustering.hclust is the square root of the within-cluster variance increase (the Ward cost is computed on squared distances, then the heights are square-rooted), so it carries a distance-like (≈ µm) scale rather than a raw variance (µm²) one. cut_threshold is passed through unchanged — there is still no simple \"nm\" interpretation, so because the height is hard to set by intuition, n_clusters is the clean choice for Ward.\nn_clusters (cut by count). Cuts the dendrogram to produce exactly that many clusters (capped at the group size), counted before the min_points filter is applied. Units-agnostic, so it works the same for any linkage.","title":"The cut and its unit convention"},{"category":"section","location":"methods/hierarchical/#Configuration","page":"Hierarchical","text":"HierarchicalConfig fields (algorithm-specific fields followed by the shared backend fields):\n\nfield default unit meaning\ncut_threshold nothing nm (distance linkages) / Ward height ≈ µm (Ward) dendrogram cut height; converted nm→µm for :single/:complete/:average, passed through unchanged for :ward\nn_clusters nothing — cut the dendrogram to exactly this many clusters (before min_points filtering)\nlinkage :ward — linkage criterion: :single, :complete, :average, or :ward\nmin_points 5 count clusters with fewer than this many members after cutting are relabeled noise (id = 0)\nuse_3d false — include the z-coordinate in the distance calculation (shared field)\nper_dataset true — cluster within each dataset independently, so (dataset, id) is unique (shared field)\nremove_unclustered false — drop noise emitters (id == 0) from the returned SMLD (shared field)\n\nExactly one of cut_threshold / n_clusters must be supplied. Supplying both, or neither, raises an ArgumentError. Additional validation: cut_threshold must be > 0, n_clusters must be ≥ 1, min_points must be ≥ 1, and linkage must be one of the four supported symbols.\n\n# Distance-based linkage: cut_threshold is in nm (converted to µm internally).\ncfg = HierarchicalConfig(cut_threshold = 200.0, linkage = :single)\n(smld_out, info) = cluster(smld, cfg)\n\n# Ward linkage: specify the number of clusters directly (units-agnostic).\ncfg_ward = HierarchicalConfig(n_clusters = 3, linkage = :ward)\n(smld_out, info) = cluster(smld, cfg_ward)","title":"Configuration"},{"category":"section","location":"methods/hierarchical/#Output-and-interpretation","page":"Hierarchical","text":"cluster(smld, cfg::HierarchicalConfig) returns a (smld_out, info) tuple. The input smld is not modified — emitters are deep-copied and cluster labels are written onto the copy's emitter.id: 0 marks noise, 1..K mark distinct clusters (per-dataset when per_dataset = true). When remove_unclustered = true, noise emitters are dropped from smld_out.\n\nThe companion info is a ClusterInfo with algorithm = :hierarchical and the usual summary fields: n_locs_in, n_clustered, n_noise, n_clusters, cluster_sizes (size of each cluster, indexed by id), and elapsed_s.\n\n(smld_out, info) = cluster(smld, HierarchicalConfig(n_clusters = 3, linkage = :ward))\ninfo.algorithm                                   # :hierarchical\nprintln(\"$(info.n_clustered)/$(info.n_locs_in) clustered into $(info.n_clusters) clusters\")","title":"Output & interpretation"},{"category":"section","location":"methods/hierarchical/#Notes-and-caveats","page":"Hierarchical","text":"Memory / scalability. The O(n^2) pairwise distance matrix is built per group, so memory grows quadratically with group size. This backend is intended for small groups; for datasets with ≫10,000 localizations per group, prefer DBSCAN.\n2D and 3D. Both are supported; set use_3d = true to include the z-coordinate (requires 3D emitters with a :z property).\nWard unit caveat. Under :ward the cut height is Clustering.jl's Ward height — the square root of the within-cluster variance increase (≈ µm scale), not a raw inter-point distance, and not nm-converted — so cut_threshold values are not comparable across linkages. When using Ward, prefer n_clusters.","title":"Notes & caveats"},{"category":"section","location":"methods/hierarchical/#References","page":"Hierarchical","text":"Clustering.jl hclust / cutree documentation: https://juliastats.org/Clustering.jl/stable/hclust.html\nWard, J. H. (1963). \"Hierarchical Grouping to Optimize an Objective Function.\" Journal of the American Statistical Association, 58(301), 236–244.","title":"References"},{"category":"section","location":"#SMLMClustering","page":"Home","text":"Clustering and spatial-statistic backends for single-molecule localization microscopy (SMLM) point data, built on SMLMData.BasicSMLD.\n\nThe package exposes three parallel verbs, each dispatched on a concrete configuration type so that the algorithm is selected by which config you pass:\n\nVerb Purpose Returns\ncluster assign a cluster label to every emitter (smld_out, ::ClusterInfo)\ncluster_statistics compute read-only spatial statistics (smld, ::ClusterStatisticsInfo)\nclassify_emitters edge / membrane / interior classification (smld, ::EdgeClassifyInfo)\n\nSee the User Guide for the calling conventions and the Methods overview for the full backend catalog and the concepts behind each one.\n\n(Image: The same localization field clustered by six backends)\n\nOne synthetic localization field run through six labeling backends — see the Methods overview.","title":"SMLMClustering"},{"category":"section","location":"#Quick-start","page":"Home","text":"using SMLMClustering\n\n# Density clustering with DBSCAN: every emitter gets a label on a deep copy.\ncfg = DBSCANConfig(eps_nm = 50.0, min_points = 5)\nsmld_out, info = cluster(smld, cfg)\n\ninfo.n_clusters        # number of clusters found\ninfo.n_noise           # emitters left unclustered (label 0)\ninfo.cluster_sizes     # size of each cluster, indexed by id\n\n# Read-only spatial statistic: is there clustering tendency at all?\n_, stats = cluster_statistics(smld, HopkinsConfig())\nstats.statistic        # Hopkins H (≈0.5 random, →1 clustered)\n\n# Edge / membrane / interior classification (2D):\n_, edge = classify_emitters(smld, KdeValleyConfig())\nedge.class             # Vector{Symbol}: :outside / :membrane / :interior\n\ncluster is non-mutating — input emitters are deep-copied and labels are written onto the copy's emitter.id (0 = noise, 1..K = clusters). cluster_statistics is pass-through — it returns the same SMLD reference unchanged alongside an info struct. classify_emitters returns a new SMLD (the input's metadata copied, with the published cell mask added under \"edge_cells\" / \"edge_outer_polygon\") alongside its info, which carries the per-emitter class.","title":"Quick start"},{"category":"section","location":"#Backend-catalog","page":"Home","text":"Labeling (cluster): DBSCAN, HDBSCAN, Hierarchical, Voronoi (SR-Tesseler), MRF density-regime, Point hysteresis.\n\nSpatial statistics (cluster_statistics): Hopkins statistic, Voronoi density, Local contrast.\n\nEdge classification (classify_emitters): Edge / Membrane Classification (OuterPolygonConfig, KdeValleyConfig).","title":"Backend catalog"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(\"SMLMClustering\")          # once registered in the General registry\n\nUntil then, install directly from GitHub:\n\nPkg.add(url = \"https://github.com/JuliaSMLM/SMLMClustering.jl\")","title":"Installation"},{"category":"section","location":"#License","page":"Home","text":"MIT. Developed in the Lidke Lab at the University of New Mexico.","title":"License"},{"category":"section","location":"methods/mrf_density/#MRF-density-regime","page":"MRF density-regime","text":"Adaptive-density clustering for 2D SMLM data that mixes density regimes — e.g. tight ~25 nm aggregates coexisting with µm-scale extended structure — where a single global ε would either over-merge the loose structure or shatter the tight knots. It is a cluster labeling backend selected by dispatch on MRFDensityClusterConfig: per-emitter density regimes are inferred, spatially smoothed, and the foreground is carved into clusters by connected components.\n\n(Image: Continuous density to coherent regimes)\n\nLeft: per-emitter density on a field with sparse background, a medium-density patch, and dense cores. Right: the MRF assigns each emitter to one of three spatially-coherent density regimes.","title":"MRF density-regime"},{"category":"section","location":"methods/mrf_density/#Concept","page":"MRF density-regime","text":"A single distance/density threshold cannot serve a dataset with several density populations. Instead of one global parameter, this backend infers a per-emitter density regime from the local point density, then enforces spatial coherence on those regime labels before extracting clusters:\n\nPer-emitter density regimes. Each emitter's local density (Voronoi 1/area or kNN k/(πr_k²)) is mapped to log ρ, and a 1D n_regimes-component Gaussian mixture is fit over log ρ. Components are sorted ascending by mean, so regime 1 is the lowest-density (background) population and n_regimes is the densest.\nSpatial smoothing via a Potts MRF. Raw per-emitter regime labels are noisy. A multi-class Potts Markov random field over the Delaunay (or kNN) neighbor graph penalizes neighboring emitters carrying different labels. This pulls borderline interior points up to foreground when their neighbors are foreground (fixes \"missing middles\") and pulls isolated tight knots in a low-density sea back down to background (kills spurious small clusters).\nClusters = connected components on the foreground. The foreground is defined as regime ≥ 2. Connected components are computed over the same neighbor graph restricted to foreground nodes; components smaller than min_points are demoted to noise.\n\nThe lowest regime is treated as background/noise; only regime ≥ 2 is fed to the connected-components step.","title":"Concept"},{"category":"section","location":"methods/mrf_density/#How-it-works","page":"MRF density-regime","text":"Step 1 — local density. Per emitter i, density is estimated in one of two ways. The Voronoi estimator uses the inverse cell area,\n\nrho_i = frac1A_i\n\nwhich is simple but noisy (σ_log ≈ 1, since it averages over a single cell) and biased downward for thin elongated structures whose cells spill across the patch boundary. The kNN estimator (Loftsgaarden & Quesenberry 1965) integrates over the density_k nearest neighbors,\n\nrho_i = frackpi r_ki^2\n\nwhere r_{k,i} is the Euclidean distance from i to its k-th nearest neighbor. This reduces noise to σ_log ≈ 1/√k and recovers patch-interior detection at the cost of some boundary blur. Each density is converted to log ρ_i; emitters with non-finite or non-positive density are dropped from the fit.\n\nStep 2 — regime assignment. A 1D n_regimes-component Gaussian mixture is fit to the valid log ρ values by EM (deterministic quantile initialization, no randomness), producing weights w_k, means μ_k, and variances σ_k² sorted ascending by mean. The data term is the per-emitter, per-regime unary cost\n\nU_ik = -logbigl(w_k mathcalN(logrho_i mid mu_k sigma_k^2)bigr)\n\nWhen regime_thresholds is supplied the GMM is bypassed and emitters are hard-binned (U_{i,k}=0 for the matching bin, 10^6 for the rest). When regime_gaussians is supplied the GMM is bypassed but the same soft Gaussian unaries above are built from the calibrated (means, vars, weights).\n\nStep 3 — Potts MRF refinement. Labels L minimize the multi-class Potts energy over the neighbor graph,\n\nE(L) = sum_i U_iL_i + lambda sum_(ij)inmathcalG mathbb1L_i neq L_j\n\ni.e. the pairwise term charges λ for every graph edge whose endpoints carry different labels. The neighbor graph \\mathcal{G} is the Delaunay triangulation (default, reusing the step-1 tessellation) or a symmetrized kNN graph (graph_kind=:knn, using graph_k). Minimization is by Iterated Conditional Modes (ICM): labels initialize to the per-emitter unary argmin, then each emitter is repeatedly reassigned to the label minimizing U_{i,k} + λ · (#neighbors with label ≠ k), sweeping until no label changes or icm_iters is reached. When smoothness_lambda is nothing, λ is auto-set per group from the spread of the unary range:\n\nlambda = maxbigl(10^-6 mathrmMAD_i(U_imax - U_imin)bigr)\n\nwhere MAD is the median absolute deviation over valid emitters — a data-scale-free heuristic that needs no per-dataset tuning.\n\nStep 4 — connected components. Foreground emitters (regime ≥ 2) are grouped by breadth-first connected components over the neighbor graph restricted to foreground nodes, so foreground points linked through a short chain of other foreground points merge into one cluster. Components below min_points are demoted to noise and the surviving clusters are compact-relabeled 1..K.\n\nWhen per_dataset=true (default) the entire pipeline runs independently per dataset, so each cell gets its own GMM fit and auto-adapts to whatever density scale it lives at.","title":"How it works"},{"category":"section","location":"methods/mrf_density/#Configuration","page":"MRF density-regime","text":"MRFDensityClusterConfig fields:\n\nfield default unit meaning\nn_regimes 2 — number of density regimes (≥ 2); regime 1 is background/noise, ≥ 2 is foreground\nregime_thresholds nothing log-density optional explicit hard-bin thresholds, length n_regimes - 1, sorted ascending; bypasses GMM (borderline points cannot be rescued by neighbors)\nregime_gaussians nothing log-density optional calibrated soft emission model (means, vars, weights); bypasses GMM but keeps soft unaries so the Potts prior can still flip borderline points\ndensity_estimator :voronoi — :voronoi = 1/cell-area; :knn = k/(πr_k²)\ndensity_k 20 — k for the kNN density estimator (:knn only); larger k → lower variance, more boundary blur; recommended 10–50\nsmoothness_lambda nothing unary-cost units Potts smoothness weight λ; nothing → auto-set per group to max(1e-6, MAD(U_max - U_min)); must be > 0 if set\ngraph_kind :delaunay — neighbor graph for MRF and CC; :delaunay reuses the Voronoi tessellation when density_estimator = :voronoi (otherwise it builds one for the graph), :knn builds a symmetrized kNN graph\ngraph_k 8 — k for the kNN neighbor graph (graph_kind=:knn only)\ninference :icm — MRF inference algorithm; only :icm supported\nicm_iters 50 passes max ICM sweeps; stops early when no label changes\nmin_points 5 emitters minimum cluster size after CC; smaller components → noise\nuse_3d false — must be false; true raises ArgumentError (2D Voronoi only)\nper_dataset true — run the full pipeline per dataset so each cell is fit independently\nremove_unclustered false — drop noise (id == 0) emitters from the output SMLD\n\nregime_thresholds and regime_gaussians are mutually exclusive; setting both raises ArgumentError.\n\n# (a) Default 2-regime auto-tuning: per-dataset GMM finds the\n#     foreground/background split, λ is auto-set, CC over Delaunay neighbors.\ncfg = MRFDensityClusterConfig()\n(smld_out, info) = cluster(smld, cfg)\nregimes = smld_out.metadata[\"mrf_regime_per_emitter\"]   # 0..n_regimes per emitter\n\n# (b) 3-regime with manual log-density thresholds (e.g. learned offline).\n#     GMM is bypassed; points are hard-binned, then CC on regime >= 2.\ncfg = MRFDensityClusterConfig(\n    n_regimes         = 3,\n    regime_thresholds = [3.5, 5.0],   # length n_regimes - 1, sorted ascending\n    min_points        = 10,\n)\n(smld_out, info) = cluster(smld, cfg)\n\n# (c) Calibrated soft emissions: fit Gaussian unaries on a trusted calibration\n#     ROI, then apply to query ROIs. Unlike hard thresholds, borderline\n#     interior points can still be rescued by high-density neighbors via Potts.\ngaussians = calibrate_regime_gaussians(calibration_smld;\n                                       n_regimes         = 2,\n                                       density_estimator = :knn,\n                                       density_k         = 20)\ncfg = MRFDensityClusterConfig(\n    density_estimator = :knn,\n    density_k         = 20,\n    regime_gaussians  = gaussians,   # NamedTuple (means, vars, weights)\n)\n(smld_out, info) = cluster(query_smld, cfg)\n\ncalibrate_regime_gaussians(smld; n_regimes=2, density_estimator=:knn, density_k=20) returns the (means, vars, weights) NamedTuple in log-density space, ready to pass as regime_gaussians. calibrate_regime_thresholds(smld; n_regimes=2, density_estimator=:knn, density_k=20) is a thin wrapper that returns instead a Vector{Float64} of length n_regimes - 1 (the analytic Bayes decision boundaries between consecutive components, midpoint fallback when the discriminant is negative), ready to pass as regime_thresholds.","title":"Configuration"},{"category":"section","location":"methods/mrf_density/#Output-and-interpretation","page":"MRF density-regime","text":"cluster returns the standard (smld_out, info::ClusterInfo) tuple. The input is never mutated — emitters are deep-copied and labels written onto the copy.\n\nemitter.id: 0 for noise (lowest regime, ungroupable, or sub-min_points component) or 1..K for cluster membership (per group when per_dataset=true).\ninfo::ClusterInfo: n_locs_in, n_clustered, n_noise, n_clusters, cluster_sizes (indexed by cluster id), algorithm == :mrf_density, and elapsed_s.\n\nMRF-specific results are stamped onto smld_out.metadata (HDBSCAN-style):\n\nkey type meaning\n\"mrf_regime_per_emitter\" Vector{Int} per-emitter regime in 0..n_regimes, original emitter order; 0 = ungroupable (group < 3 points or too few valid densities), 1 = lowest density / background, n_regimes = highest density. On GMM failure all valid emitters fall back to regime 1, so the group yields no clusters.\n\"mrf_lambda_used\" Vector{Float64} per-group λ actually used (auto or explicit), in dataset-group order; NaN for skipped groups\n\"mrf_regime_means\" Vector{Vector{Float64}} per-group GMM component means in log-density space (sorted ascending); a vector of NaNs of length n_regimes when regime_thresholds was supplied (signals \"manual hard binning, no fit\"); calibrated means when regime_gaussians was supplied\n\nForeground → clusters. Emitters with regime ≥ 2 form the foreground; connected components on that foreground (over the neighbor graph) become the clusters, after the min_points filter. So mrf_regime_per_emitter[i] ≥ 2 does not by itself guarantee emitter.id > 0 — the emitter must also land in a surviving (large-enough) connected component.","title":"Output & interpretation"},{"category":"section","location":"methods/mrf_density/#When-it-works-/-accuracy","page":"MRF density-regime","text":"Characterized on a controlled 5×5 µm A431-mimic synthetic (kNN density estimator, density_k=20, 2 regimes, ~13–22 k emitters across 12 patches, aspect ratios 1–20). Headline accuracy by density ratio (high / low):\n\nratio kNN-MRF voronoi-GMM (no MRF)\n1.2× 35% 65%\n1.5× 69% 67%\n2.0× 89% 72%\n3.0× 95% 69%\n5.0× 96% 87%\n\nUse kNN-MRF when the density ratio is ≥ 2×. Operational floor: ratio ≥ 1.65× clears 75% accuracy; ratio ≥ 1.85× clears 85%. Below ~1.65× the two log ρ modes nearly overlap, GMM EM picks a degenerate split, and the Potts smoothness term amplifies the majority label across the graph — a false-positive saturation collapse (not a graceful recall loss).\n\nkNN vs Voronoi-GMM. The Voronoi default preserves backward compatibility and is the right estimator for blob-shaped clusters. For thin elongated structures (widths comparable to the local nearest-neighbor distance — fibers narrower than ~150 nm at high density) Voronoi cells spill across the patch boundary and produce a patch-interior false-negative band; switch to density_estimator=:knn (with density_k=20) to recover interior detection. Because the MRF lacks the per-emitter independence of plain voronoi-GMM, it degrades catastrophically rather than gracefully below the density-ratio floor; at low contrast (ratio < 2×) prefer voronoi-GMM or calibrated soft emissions.\n\nUse calibrated soft emissions (calibrate_regime_gaussians + regime_gaussians) when calibration ROIs are available and the density ratio is below 2×. This skips per-ROI GMM degeneracy while keeping soft unary costs, so the MRF can still fill borderline interior points. Hard regime_thresholds also stabilize low-contrast data but forfeit neighbor-rescue (the 10^6 penalty dwarfs the pairwise term).\n\nkNN ball-radius bound. The kNN estimator assumes its ball radius r_k ≈ √(k/(πρ)) is smaller than the structure half-width; otherwise the ball spills into background and the GMM regime split can flip foreground/background. For thin patches where density_k=20 is too coarse, drop to density_k=8–10. At 2× contrast the patch-size operating range is: ≥ 1.0 µm holds 88–89% (85% gate), ≥ 0.5 µm clears the 75% gate, and ≤ 0.3 µm collapses (ball spillage). Within k ∈ [5, 80] the accuracy curve is concave-up with a shallow peak at k = 15–20, so the density_k=20 default is robust.","title":"When it works / accuracy"},{"category":"section","location":"methods/mrf_density/#Notes-and-caveats","page":"MRF density-regime","text":"2D only. use_3d=true raises ArgumentError (DelaunayTriangulation.jl provides no 3D Voronoi). Use DBSCANConfig or HierarchicalConfig for 3D.\nGroups with fewer than 3 points are tagged all-noise (too small to tessellate) and recorded as λ = NaN, means = NaN.\nExact-duplicate (x, y) coordinates raise ArgumentError only when a Voronoi tessellation is actually built — i.e. density_estimator = :voronoi or graph_kind = :delaunay. With density_estimator = :knn and graph_kind = :knn, no Voronoi guard runs and duplicates are not rejected.\nPer-dataset GMM fit. With per_dataset=true each dataset is fit and clustered independently; (dataset, id) uniquely identifies a cluster.\nGMM-failure fallback. If the EM fit collapses (variance collapse, all-equal densities, component starvation) the group's valid emitters fall back to regime 1 (background) and contribute no clusters.\nLocal MAP inference. ICM is local MAP, not a global optimizer; pathological low-contrast data without a representative calibration ROI can still be fragile.","title":"Notes & caveats"},{"category":"section","location":"methods/mrf_density/#References","page":"MRF density-regime","text":"Potts model — the multi-class generalization of the Ising model, used here as the pairwise smoothness prior over the neighbor graph.\nIterated Conditional Modes (ICM) — the local MAP inference used to minimize the Potts energy.\nLoftsgaarden, D. O. & Quesenberry, C. P. (1965) — the k-nearest-neighbor density estimator ρ_k = k/(πr_k²).","title":"References"},{"category":"section","location":"methods/#Methods-overview","page":"Overview","text":"SMLMClustering bundles several spatial-analysis backends behind three verbs. This page is the map: what each backend is, the family of algorithm it belongs to, and how to choose between them. Each method then has its own page with the concept, the math, the configuration, and a worked example.","title":"Methods overview"},{"category":"section","location":"methods/#Catalog","page":"Overview","text":"","title":"Catalog"},{"category":"section","location":"methods/#Labeling-—-cluster","page":"Overview","text":"Assign a cluster id to every emitter (0 = noise, 1..K = clusters).\n\nMethod Family 2D/3D Scales to large n?\nDBSCAN density / ε-neighborhood 2D + 3D yes (KD-tree)\nHDBSCAN hierarchical density 2D + 3D moderate\nHierarchical agglomerative linkage 2D + 3D no (O(n²) per group)\nVoronoi (SR-Tesseler) tessellation density 2D only yes\nMRF density-regime GMM + Potts MRF on a neighbor graph 2D only yes\nPoint hysteresis seed-and-grow on a kNN graph 2D + 3D yes","title":"Labeling — cluster"},{"category":"section","location":"methods/#Spatial-statistics-—-cluster_statistics","page":"Overview","text":"Read-only summaries; the SMLD is returned untouched.\n\nMethod Reports\nHopkins statistic clustering tendency H (is there structure at all?)\nVoronoi density per-emitter Voronoi cell area + local density ρ = 1/A\nLocal contrast per-emitter local-vs-neighborhood density contrast feature","title":"Spatial statistics — cluster_statistics"},{"category":"section","location":"methods/#Edge-classification-—-classify_emitters","page":"Overview","text":"Method Reports\nEdge / Membrane Classification per-emitter :outside / :membrane / :interior","title":"Edge classification — classify_emitters"},{"category":"section","location":"methods/#Labeling-vs.-classification","page":"Overview","text":"All three verbs attach per-emitter information, but they are different kinds of operation and — by design — store their answers in different places:\n\ncluster assigns an instance label. It writes an integer onto each emitter.id (0 = noise, 1..K = clusters); K is discovered from the data and the ids are only meaningful within a run. This is instance grouping: \"which cluster does this point belong to?\"\nclassify_emitters assigns a semantic class. It leaves emitter.id untouched and stores the per-emitter class — a fixed vocabulary (:outside / :membrane / :interior) — in info.class (read via in_cell / interior_mask), threading only the boundary geometry (edge_cells / edge_outer_polygon) into smld.metadata. This is semantic labeling: \"what kind of region is this point in?\" — stable and self-describing across runs.\ncluster_statistics assigns nothing. It is read-only and returns summary scalars/vectors, leaving the SMLD unchanged.\n\nBecause labeling and classification write to different fields, they compose — classify into a region, then cluster within it (or cluster, then ask which clusters fall in the membrane). If both wrote to emitter.id, the second step would overwrite the first.\n\n# Classify, keep the cell interior, then cluster within it.\n_, edge = classify_emitters(smld, KdeValleyConfig())\nkeep    = [e for (e, c) in zip(smld.emitters, edge.class) if c === :interior]\ninner   = SMLMData.BasicSMLD(keep, smld.camera, smld.n_frames, smld.n_datasets, smld.metadata)\ninner_out, info = cluster(inner, DBSCANConfig(eps_nm = 50.0, min_points = 5))","title":"Labeling vs. classification"},{"category":"section","location":"methods/#Concepts-and-vocabulary","page":"Overview","text":"Emitter / localization. A single blinking event with a position (and uncertainty). The input is an SMLMData.BasicSMLD; positions are in µm, but most clustering parameters that have a physical length are specified in nm for convenience (each backend page states its units explicitly).\nCluster id. Labeling backends write an integer onto each emitter.id: 0 marks noise / unclustered points, and 1..K mark distinct clusters.\nmin_points. For most labeling backends, the minimum number of emitters a cluster must contain to count as real rather than noise. HDBSCAN is the exception: there min_points is the core-distance neighbor count k (the density-smoothing scale), while a separate min_cluster_size sets the size threshold.\nper_dataset. SMLD data can hold several datasets (e.g. cells / ROIs). When per_dataset = true (the default) each dataset is clustered independently and the pair (dataset, id) uniquely identifies a cluster; ids are local to a dataset.\nDensity-based vs. tessellation vs. hierarchical. Density methods (DBSCAN, Point hysteresis) grow clusters from regions where points are closer than a length scale. Tessellation methods (Voronoi (SR-Tesseler), Voronoi density) turn each point's Voronoi-cell area into a local density and threshold on it. Hierarchical methods (Hierarchical, HDBSCAN) build a tree of merges and cut it. The MRF density-regime backend adds a smoothness prior over a neighbor graph so density labels are spatially coherent.\nClustering tendency. Before clustering, the Hopkins statistic asks whether the data is clustered at all, versus consistent with spatial randomness.","title":"Concepts & vocabulary"},{"category":"section","location":"methods/#Choosing-a-backend","page":"Overview","text":"(Image: The same field clustered by six backends)\n\nOne synthetic field, six backends. Unclustered noise (id = 0) is gray. Density methods isolate the clusters and reject noise; HDBSCAN over-splits here; hierarchical cut to a fixed count has no noise class, so it absorbs the background into the nearest cluster.\n\nStart with DBSCAN — it scales, works in 2D and 3D, has no O(n²) memory cost, and has only two intuitive knobs (eps_nm, min_points).\nSingle global ε fails when tight aggregates coexist with extended structure → use the MRF density-regime backend, which infers per-emitter density regimes.\nWant a parameter-light, calibration-free 2D density rule following SR-Tesseler → Voronoi (SR-Tesseler).\nSmall groups, want a specific cluster count or a dendrogram → Hierarchical.\nJust need a density feature for your own downstream thresholding → Voronoi density or Local contrast via cluster_statistics.\nWant to know if clustering is even present → Hopkins statistic.\n\nSee the User Guide for the shared calling conventions and the ClusterInfo / ClusterStatisticsInfo result fields.","title":"Choosing a backend"}]
}
